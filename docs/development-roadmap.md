# 开发路线与验收标准

> 最后更新：2026-07-31
> 用法：本文件保存完整路线和阶段验收，不是每步全文必读材料。任何新开发窗口先读 `AGENTS.md` 和 `docs/handoff.md`，再只读取handoff指定的当前阶段；一次只执行一个小任务，不回放已完成阶段。

当前任务入口只在 `docs/handoff.md` 维护。本文件只在阶段状态变化时更新，具体运行数字、临时故障和逐次验证流水不在这里重复。

## 1. 状态说明

- `[完成]`：代码、自动化测试和必要的真实验证均通过。
- `[进行中]`：当前允许开发的阶段。
- `[待开始]`：已设计，但不能写成已实现功能。
- `[可选]`：不影响简历主项目完成度。

## 2. 总体顺序

```text
阶段 1-4：MVP（已完成）
-> 阶段 5：登录、用户隔离与管理员管理（已完成）
   -> 5.5 Vue 登录体验
   -> 5.6 管理员与系统文档管理
-> 阶段 6：Redis 接口保护
-> 阶段 7：RAG 评估与检索优化
-> 阶段 8：可观测性
-> 阶段 9：用户治理、资料审核与管理中台
-> 阶段 10：Docker 与云端发布（已完成）
-> 阶段 11：基于 LangGraph 的受控资料 Agent（已完成）
-> 阶段 12：知识质量、记忆与文档智能（已完成）
-> 阶段 13：Codex 式对话 Agent 工作台（已完成）
-> 阶段 14：Agent Chat v3.1 产品修复（已完成）
-> 阶段 15：Agent Chat v3.2 执行时间线与界面稳定化（已完成）
-> 阶段 16：邮箱账号生命周期与模型用量计量（已完成）
-> 阶段 17：自动长期记忆 v2（已完成并发布，自动提取开关关闭）
-> 阶段 18：用户额度与用量中心（已完成并发布，额度执行开关关闭）
-> 阶段 19：额度策略v2（100万月额度、shadow与动态预留）
```

一次只完成一个可验证的小任务。禁止同时改认证、Redis、RAG 和 Agent。

对应发布里程碑：

- `RAG v1.1`：阶段 6 完成，具备限流、并发保护和幂等性。
- `RAG v1.2`：阶段 7 完成，检索效果可以用固定评估集证明。
- `RAG v1.2.1`：任务 7.7 完成候选池补充实验，任务 7.8 完成一次受控里程碑发布；是否启用新检索方案必须服从真实证据。
- `RAG v1.3`：阶段 8 完成，可以解释请求耗时、费用和失败原因。
- `Platform v1.4`：阶段 9 完成，具备三层角色、资料审核、个人中心和管理中台。
- `Cloud v2.1`：阶段 10 完成，可从空服务器重复部署并安全保留数据。
- `Agent v2.2`：阶段 11 完成，具备独立、受控、可审计的资料整理 Agent。
- `Knowledge v2.3`：阶段 12 完成，具备质量反馈、可控记忆和复杂文档演进能力。
- `Agent Chat v3.0`：阶段 13 完成，具备多轮会话、可见执行过程、连续任务和产物工作区。
- `Agent Chat v3.1`：阶段 14 完成，具备确定消息顺序、聊天优先界面、按需工具循环和
  已退出旧任务入口与旧记录的稳定产品主链路。
- `Agent Chat v3.2`：阶段15已完成并发布，具备运行时展开、结束后折叠的公开
  “计划-工具-检查”时间线、RAG一致的聊天与引用视觉，以及删除会话后的严格状态清理；
  生产发布与无费用主路径证据已归档。
- `Platform v1.5`：阶段16完成后，具备邮箱验证码注册、忘记密码、旧JWT失效、可审计的
  测试账号清理，以及普通RAG和Agent的可靠Token/估算费用计量。
- `Memory v2.0`：阶段17完成后，具备自动候选提取、敏感审核、冲突历史和RAG/Agent统一
  相关记忆。
- `Usage v2.0`：阶段18完成后，具备回答级计量、个人/管理员用量中心和并发安全额度。
- `Quota v2.1`：阶段19完成后，具备100万默认月额度、三级执行模式、动态预留、可选
  费用闸门和三角色额度管理。

这些里程碑名称表示能力边界，不要求每次都立刻部署。这里的“对标高星项目”指学习清晰模块、运行状态、评估、审计、知识治理和故障边界，不是复制 Dify、RAGFlow、Coze、Langflow 或 OpenHands 的全部平台功能。

## 3. 已完成阶段

### 阶段 1：FastAPI 基础 `[完成]`

- 应用工厂、配置、异常处理、CORS。
- 低成本健康检查和 OpenAPI 文档。
- pytest 基础测试。

### 阶段 2：基础 RAG `[完成]`

- 通义千问与 DashScope Embedding。
- Chroma 持久化检索。
- 普通问答、引用来源和知识不足拒答。

### 阶段 3：知识库管理 `[完成]`

- PDF/TXT 上传、大小和格式校验。
- 递归切片、SHA-256 去重、向量入库。
- 文档列表和文件/登记/向量一致删除。
- 批量导入脚本已经存在于 `backend/scripts/`，目前尚未提交 Git。

### 阶段 4：Vue、SSE 和会话历史 `[完成]`

- Vue 3 概览、知识库、问答页面。
- SSE 逐块显示、停止生成、安全错误事件。
- MySQL 会话、消息、来源持久化。
- 最近 3 轮上下文、刷新恢复、会话增删改查。
- 前端组件、SSE 分片和后端测试。

## 4. 阶段五：登录和用户隔离 `[完成]`

### 4.1 固定产品决策

- 邮箱和密码注册登录。
- 每个账号只看到自己的历史会话。
- 当前旧会话是测试数据，迁移时备份后清空。
- 知识库全部公共，登录用户都可检索。
- 新上传文档也公开，但只有上传者可以删除。
- 系统预置文档不能由普通用户删除。
- 第一版只做短期 Bearer JWT。
- 基础登录版本不做邮箱验证码、找回密码和 Refresh Token。
- 完成 Vue 登录后增加管理员角色；当前普通用户上传公共资料的规则保持不变。

### 4.2 建议拆分任务

#### 任务 5.1：迁移基础和用户模型 `[完成]`

范围：

1. 引入 Alembic，并为当前三张会话表生成可复现基线。
2. 新建 `modules/auth` 的 model、schema、repository、service 骨架。
3. 创建 `users` 表，邮箱唯一，密码只保存哈希。
4. 增加迁移和模型测试。
5. 不修改现有页面，不接 JWT，不清空会话。

验收：

- 空数据库可通过 Alembic 升级到最新结构。
- 已有数据库可执行升级且原三张表数据不丢失。
- 重复邮箱被数据库和服务层正确拦截。
- 数据库和日志中都没有明文密码。
- 后端原 31 项测试仍通过，并新增认证模型与迁移测试，当前共 37 项。

#### 任务 5.2：注册、登录和当前用户接口 `[完成]`

范围：

- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `GET /api/v1/auth/me`
- 密码哈希、JWT 签发和认证依赖。

验收：

- 正确注册登录可获得 Token。
- 错误邮箱/密码只返回统一安全提示。
- 过期、伪造、缺失 Token 均返回 401。
- API Key、JWT 密钥和密码不进入 Git 或日志。

#### 任务 5.3：会话归属和旧数据迁移 `[完成]`

范围：

- `conversations` 增加 `user_id`。
- 迁移前导出旧测试会话，随后按用户决策清空。
- 会话所有查询和操作绑定当前用户。

验收：

- A 用户看不到、读不到、改不了、删不了 B 用户会话。
- 猜测其他会话 UUID 仍返回 404，不泄漏资源存在。
- SSE 普通完成、失败、中止状态仍正确落库。

#### 任务 5.4：文档登记迁移和公共权限 `[完成]`

范围：

- 建立 `documents` 表，替代 `documents.json` 为主登记源。
- 现有文档迁移为系统公共文档。
- 新上传记录 `uploader_id`。
- 公共检索、上传者删除、系统文档保护。

验收：

- 迁移前后文档数量和 Chroma 片段数量一致。
- 不重新调用 Embedding，不产生重复片段。
- 任意登录用户可检索公共文档。
- 非上传者不能删除，系统文档不能由普通用户删除。
- 删除仍验证 MySQL、文件、Chroma 三处一致性。

#### 任务 5.5：Vue 登录体验 `[完成]`

范围：

- 注册和登录页。
- Token 管理、当前用户状态和退出登录。
- 路由守卫和 401 统一处理。
- 登录后加载当前用户会话。

验收：

- 未登录访问问答/知识库会跳到登录页。
- 登录成功返回原目标页面。
- 刷新后在 Token 有效期内保持登录。
- 退出后清空本地凭据和当前会话状态。
- 浏览器控制台无错误，前端测试和构建通过。

#### 任务 5.6：管理员与系统文档管理 `[完成]`

范围：

- 通过 Alembic 为 `users` 增加 `role`，第一版只允许 `user` 和 `admin`。
- 增加统一的 `require_admin` 认证依赖，管理员权限必须由后端校验。
- JWT 仍只保存用户 ID，角色以数据库为准；`/auth/me` 返回角色供前端展示。
- 提供系统文档新增、删除和整份替换接口。
- 普通文档与系统文档共用一个文档生命周期组件，禁止复制解析、切片、向量化、快照和回滚逻辑。
- 增加独立的管理员知识库页面，不把管理逻辑塞进普通问答页面。
- 修复 `backend/scripts/import_documents.py`，让它显式使用管理员身份或受控维护服务，不绕过正常文档流程。
- 普通登录用户仍可上传公开资料并删除自己的上传；管理员额外负责系统文档。

验收：

- 新注册用户默认是 `user`，不能伪造或自行提升为 `admin`。
- 管理员初始化采用受控命令或安全配置，不提供公开的管理员注册接口。
- 普通用户调用管理员接口统一返回 403。
- 修改 localStorage、JWT载荷或请求参数都不能获得管理员权限；用户降权后旧Token立即失去管理员能力。
- 管理员新增系统文档后，所有登录用户都能检索且普通用户不能删除。
- 系统文档替换采用新版本先就绪、旧版本后清理的可补偿流程，不宣称跨存储原子事务。
- 新增、删除或替换任一步失败时，三处数据恢复到操作前状态。
- 同一文档的并发替换被拒绝或串行执行；不能出现两个管理员互相覆盖。
- 批量导入脚本可重复运行，重复内容跳过，且不绕过哈希去重、切片和向量登记。
- 管理页面完成组件测试和真实浏览器验收。
- 普通注册登录、会话隔离、普通上传删除、RAG检索和SSE回归全部通过，任务5.6不得改变这些接口契约。

### 4.3 阶段完成标准

- 两个账号的数据隔离测试通过。
- 公共文档检索和删除权限测试通过。
- 普通用户与管理员的系统文档权限测试通过。
- 现有 RAG、SSE、刷新恢复和停止生成无回归。
- README 和演示步骤更新。

## 5. 阶段六：Redis 接口保护 `[完成]`

### 建议拆分任务

1. **任务 6.1 `[完成]`**：增加 Redis 配置、连接适配器、健康状态和可测试的故障策略，不接入业务限流或锁。
2. **任务 6.2a `[完成]`**：只为注册和登录增加按 IP 的有界限流及统一 429 响应。
3. **任务 6.2b `[完成]`**：只为普通聊天和会话聊天增加按用户的频率保护，不改上传和生成锁。
4. **任务 6.2c `[完成]`**：为上传增加可组合的频率与并发保护，不改文档生命周期；普通用户同时受频率和并发限制，管理员系统文档上传只保留并发限制。
5. **任务 6.3a `[完成]`**：增加同一用户同一会话的生成锁，验证所有权令牌、TTL、中止和异常释放。
6. **任务 6.3b `[完成]`**：增加客户端请求 ID 和幂等记录，防止网络重试重复创建消息或重复调用模型。
7. **任务 6.4a `[完成]`**：完成 Redis 保护功能的故障矩阵、结构化日志与可观测状态，不改页面。
8. **任务 6.4b `[完成]`**：前端稳定提示、可取消的后端模型流、真实 Redis/HTTP/页面复测均通过，`RAG v1.1` 已冻结。

任务 6.1 验收：

- Redis 地址和超时只从环境配置读取，缺失配置不会导致与 Redis 无关的接口无法启动。
- 连接、ping、关闭和健康状态由 Infrastructure 适配器集中封装，路由和业务 Service 不直接创建 Redis 客户端。
- 未配置、连接失败和运行中断都有明确状态、有限超时、稳定日志与自动化测试。
- 健康接口能区分“应用可运行但 Redis 未启用/降级”和“Redis 正常”，不输出连接密钥。
- 后端完整回归通过，且代码中尚未出现限流计数、会话锁或幂等键业务逻辑。

任务 6.2a 验收：

- 注册和登录分别使用明确的按 IP 窗口与上限，计数键有 TTL，不会无限增长。
- 只有受信代理配置启用时才读取转发地址，默认使用直接连接地址，避免伪造请求头绕过限流。
- 超限稳定返回 429、统一错误码、`request_id` 和合理的 `Retry-After`，不泄漏账号是否存在。
- Redis 不可用时采用明确的安全降级并记录告警，不能无声放开无限登录尝试。
- 注册登录原有成功、失败和安全提示测试保持通过；聊天、上传和生成流程没有改动。

任务 6.2b 验收：

- 普通聊天、会话非流式问答和 SSE 问答都在调用检索或模型前按当前用户消费额度，不遗漏任一入口。
- 用户键使用稳定摘要，不包含 Token、邮箱、问题正文或其他敏感信息；不同用户额度互不影响。
- 非流式和 SSE 接口均能在响应开始前返回统一 429、`request_id` 与 `Retry-After`，不把限流错误伪装成成功流事件。
- Redis 不可用时继续使用有容量和自动过期上限的本机兜底并记录降级，多实例限制需在文档中如实说明。
- 原聊天成功、失败、停止生成和历史恢复测试保持通过；上传、生成锁、幂等、Prompt 和检索算法没有改动。

任务 6.2c 验收：

- 普通上传在解析、Embedding、文件和 Chroma 写入前按当前用户检查频率，并限制同一用户同时进行的上传数量；管理员系统文档上传跳过频率计数，但仍执行同一套并发租约保护。
- 频率键和并发占位键使用独立命名空间、稳定用户摘要和有限 TTL，不包含 Token、邮箱、文件名或文档正文。
- 超限或并发占满返回稳定 429、`request_id` 与合理的 `Retry-After`；拒绝请求不产生临时文件、向量或数据库登记。
- Redis 故障策略明确且有测试，不能静默放开无限昂贵上传；并发占位的释放必须覆盖成功、解析失败、Embedding 失败和客户端异常。
- 原上传去重、权限、跨 MySQL/文件/Chroma 补偿测试保持通过；不修改共享文档生命周期、聊天、生成锁、Prompt 或检索算法。

任务 6.3a 验收：

- 会话非流式和 SSE 问答在保存消息、检索和模型调用前，以“当前用户 + 会话”为键获取同一把生成锁；普通无会话问答暂不接入。
- 锁使用随机所有权令牌和有限 TTL，释放时原子比较令牌，旧请求不能删除后来请求获得的锁。
- 已占用时返回稳定冲突错误与 `request_id`；Redis 未配置、连接失败或锁状态不明确时返回明确临时不可用，不能继续产生模型费用。
- 非流式成功/失败，以及 SSE 完成、模型失败、主动停止、客户端断开和生成器关闭都释放锁；进程崩溃由 TTL 最终回收。
- 原会话归属、消息状态、历史恢复、聊天限流和上传保护测试保持通过；不同时实现客户端请求 ID、幂等记录或修改 RAG 行为。

任务 6.3b 验收：

- 会话非流式和 SSE 问答接受受约束的客户端请求 ID；幂等键绑定当前用户、接口和请求 ID，不包含 Token、问题正文或邮箱。
- 同一个幂等请求重复到达时不重复保存消息、检索或调用模型，并能返回原有资源标识或稳定结果摘要；不缓存完整医疗回答。
- 首次请求进行中、成功、业务失败、客户端断开以及幂等存储故障都有明确状态、有限有效期和自动化测试，不留下永久卡死记录。
- 幂等检查与会话生成锁的顺序明确，既不能绕过生成锁，也不能把正常网络重试错误地当作另一个新生成任务。
- 原会话归属、消息状态、历史恢复、聊天限流、生成锁和上传保护测试保持通过；不修改 RAG Prompt、检索算法或普通无会话问答。

任务 6.4a 验收：

- 用一张可测试的故障矩阵覆盖 Redis 未配置、连接失败、命令失败、恢复、锁状态不明确和幂等结果登记失败，明确每种功能是本机兜底还是 fail-closed。
- 结构化日志和可观测状态只记录功能、结果、降级原因类型和必要计数，不记录 Redis URL、Token、邮箱、问题、回答或文档正文。
- 健康或独立状态接口能区分限流兜底、生成锁不可用和幂等不可用，不把应用仍存活误报为整体宕机。
- 自动化测试证明日志不会重复刷屏、故障恢复可见且不会因为观测代码改变原 429/409/503 行为。
- 不安装 Redis、不修改 Vue 页面、不调用模型，也不修改 RAG、上传生命周期、Agent 或 Docker。

任务 6.4b 验收：

- 前端对 429、生成冲突 409、幂等进行中/复用冲突和 Redis 临时不可用 503 给出稳定、可理解且不过度技术化的提示。
- 在用户确认的可用 Redis 环境中完成一次真实 HTTP 和浏览器验收，证明首次生成、重复请求恢复、并发冲突与停止生成符合契约。
- 停止生成不能只依赖浏览器断开：前端发送带本次请求标识的停止请求，后端保存 `stopped`、清理幂等状态并释放生成锁后，页面才恢复下一次发送。
- 模型下一块数据长时间未返回时，停止仍须取消本地异步读取并关闭 DashScope HTTP 流；自动化阻塞测试不能只验证数据块之间的取消标记。
- 冻结阶段六配置、错误码、文档和演示步骤，完整回归通过后标记 `RAG v1.1`。

后续任务共同边界：

- `RateLimitService`、`DistributedLockPort` 和 Redis Adapter 分层，Router/业务 Service 不执行 Redis 命令。
- 限流、锁和幂等使用独立键空间与 TTL，不在键中暴露邮箱、Token 或问题正文。
- Redis 只保存可重建的短期状态，不保存用户、会话、文档或 Agent 运行的唯一副本。
- 锁释放必须验证所有权令牌，Redis 状态不明确时不得继续产生昂贵模型调用。
- 每小步只修改对应接口，不能顺手缓存回答、改 RAG Prompt 或引入消息队列。

### 范围

- Redis 连接和健康状态。
- 注册、登录、聊天、上传按 IP/用户限流。
- 同一会话生成锁。
- 重复请求幂等保护。
- 429 错误结构和前端提示。

### 暂不做

- 不缓存最终医疗回答。
- 不引入复杂消息队列。
- 不把 Session 或业务主数据只存在 Redis。

### 验收

- 超限请求稳定返回 429 和 request_id。
- 不同用户的额度互不影响。
- 同一会话不能并行生成两次。
- 锁有超时且异常后可释放。
- Redis 故障行为有测试和日志。

## 6. 阶段七：RAG 评估与检索优化 `[已完成]`

### 建议拆分任务

1. **任务 7.1 `[已完成]`**：已完成 `corpus_v1`、40题 `eval_v1`、checksum 强绑定、离线 Runner、假适配器、只读真实适配器、两次有界真实运行和受控本地人工复核。原始 `current_baseline_v1` 保持不变；第二次捕获40题全部成功，Qwen 计量为79893 Token、估算0.272745元，Embedding 最坏预留不超过0.01024元。逐题语义复核把行为准确率从自动0.8修正为1.0、拒答准确率从0.333333修正为1.0、平均关键事实覆盖从自动0修正为0.916667；同时发现“拒绝后仍泄露一般剂量”和“关键事实正确但额外内容未做忠实性核验”等局限。回答正文只存在 Git 忽略目录并在7天后删除，版本化仓库只保存不含正文的决定与摘要。本任务未修改检索、Prompt、`eval_v1`、数据库、前端或原始报告。
2. **任务 7.2 `[已完成]`**：`RagService` 保持普通、同步流和异步流统一入口，内部通过 `QueryBuilderPort`、`KnowledgeSearchPort`、`AnswerGeneratorPort` 编排。当前适配器原样包装最近历史补全规则、Chroma检索和Qwen三种生成方式；统一检索片段不暴露LangChain对象。Prompt、`top_k=4`、Embedding、集合、拒答、来源和异常契约均未改变，接口隔离测试证明三个能力可分别替换。
3. **任务 7.3 `[已完成]`**：`KnowledgeSearchOptions` 提供科室、主题、文档类型、知识库版本四类精确元数据过滤和可选0～1最低相关度；默认全部关闭并继续走原无分数向量检索。显式启用阈值后只保留达标片段，无合格片段时由集中 `RagRetrievalPolicy` 返回可配置知识不足文案且不调用回答模型。新上传片段补充可核验的文档类型和知识库版本；未回写现有103个片段，也不根据文件名编造科室/主题。Mock测试覆盖单/多条件Chroma过滤、阈值边界、默认回退、拒答和三类问答入口。
4. **任务 7.4 `[已完成]`**：新增独立 `KeywordSearchPort`，从Chroma只读加载匹配片段并以无额外依赖的BM25式关键词分数排序；中文使用单字+二元组，英文按规范化词元。`HybridKnowledgeSearchAdapter` 使用加权RRF融合向量与关键词排名，以文档ID+正文哈希去重；默认开关关闭时只组装原向量适配器。关键词侧失败记录异常类型并原样回退向量顺序，向量侧失败继续按原异常契约上抛。本步未引入Reranker、真实调用或生产参数调优。
5. **任务 7.5 `[已完成]`**：新增独立 `RerankPort` 与厂商无关结果/计量对象，在召回后、回答前由统一 `RerankStage` 编排。默认开关关闭时不创建或调用DashScope适配器；开启后最多取10个候选、一次调用、3秒超时，并以12000保守Token和0.01元预估费用为硬边界。超预算不调用，超时、失败、无效响应或计量越界均记录脱敏原因并原样回退候选。厂商SDK只存在于基础设施适配器，普通、同步流和异步流继续共用同一入口。本任务只用Mock验证，没有真实Rerank、Embedding或Qwen费用，也未修改Prompt、检索公式、API、数据库或前端。
6. **任务 7.6 `[已完成]`**：三套候选使用同一 `eval_v1`、语料checksum、Prompt、`top_k=4`和评分方法完成真实对比；冻结向量基线未重复调用，两个新候选各40题全部成功，合计估算费用0.622606元。`hybrid_rrf_v1` 与 `hybrid_rrf_rerank_v1` 的来源平均召回率均为0.919643、完整命中率均为0.785714，与向量基线完全相同；前者总耗时增加4.50%、费用增加1.30%，后者总耗时增加8.92%、Token增加71.01%、费用增加18.65%。自动拒答只多正确1题且无法排除生成波动，关键事实精确子串仍为0，不能证明质量收益。因此生产继续使用向量基线，混合检索与Reranker保持默认关闭；实验Port和适配器只作为可复现、可测试模块保留，不让无收益复杂度进入运行时主链路，`RAG v1.2` 已冻结。
7. **任务 7.7 `[已完成：没有候选晋级]`**：首次运行的真实端口装配错误已保留审计并修复；用户重新确认后完成有效恢复运行，40题向量、关键词和Reranker全部成功，0次Qwen、无失败回退。参考方案总体`Recall@4=0.919643`、多文档`Recall@4=0.718750`。文档配额方案总体提高到0.931548，但多文档仅提高0.041667，未达0.05门槛；两个混合方案多文档提高到0.791667，但总体降至0.904762，违反总体不回退条件。因此没有候选进入完整回答评估，不事后修改黄金标准或放宽门槛；生产继续使用向量基线，混合检索与Reranker保持关闭。
8. **任务 7.8 `[已完成]`**：完成`RAG v1.2.1`里程碑发布和受控线上验收。本地发布审计、单一可回滚提交、GitHub推送、四类数据备份、指定提交同步和后端重建已完成；生产继续使用冻结向量基线，混合检索与Reranker关闭。最终验收以实时27份文档/103个Chroma片段/27个文件、4个账号/5个会话/40条消息为基线，使用3次Embedding业务调用和2次Qwen SSE、0次Reranker、0次自动重试：上传后三处同步变为28/104/28；短回答有19个token事件、4个来源，医学事实、上传来源和`done`全部命中；长回答首个token后73毫秒完成主动停止，消息状态为`completed,stopped`，生成锁释放、停止状态回到`idle`且同会话幂等回放成功。清理后全部数量恢复，账号/会话/文档/文件/Chroma五类残留均为0，四容器健康。自动浏览器未能取得纯IP HTTP页面的可视证据，该限制继续保留。

### 验收

- 来源命中率和拒答准确率可量化。
- 对比报告同时记录质量、耗时和模型费用。
- Reranker 关闭时基础检索仍可运行。
- 检索变化不影响会话持久化和 SSE 协议。
- 评估运行不写入真实用户会话，也不修改知识库生产数据。
- 每项优化都能单独开关和回退，不能同时更换切片、Embedding、召回和 Prompt 后再猜哪项有效。
- 候选池数量和最终上下文数量必须是两个独立概念；Reranker不能只对最终4个片段重新排列后宣称提高召回。
- 文档级排序指标按每个文档首次出现位置计算，同一文档的重复片段不能虚增MRR或nDCG。
- 任务7.7第一轮不调用回答模型；未达到预先登记的提升门槛不得进入第二轮完整回答评估。
- 任务7.8发布前必须留下可回滚的Git提交和线上数据备份，不能直接在服务器修改业务代码。

## 7. 阶段八：可观测性 `[已完成并部署]`

### 建议拆分任务

1. **任务 8.1 `[已完成]`**：统一 JSON 日志字段和 request_id 上下文，不修改业务结果。
2. **任务 8.2 `[已完成]`**：记录请求、检索、重排、模型四类阶段耗时和稳定错误类型。
3. **任务 8.3 `[已完成]`**：记录 Token、估算费用、限流、Redis 降级、中止和失败指标。
4. **任务 8.4 `[已完成]`**：增加管理员只读统计接口与简单页面，不开放日志全文和敏感正文。
5. **任务 8.5 `[已完成]`**：补故障注入和保留/轮转策略，形成并发布 `RAG v1.3`。

### 验收

- 可使用 request_id 串起一次请求。
- 日志不包含密钥、密码、Token 或完整医学正文。
- 能解释一次慢请求耗时在哪个阶段。
- 能查看成功率、平均耗时和主要失败原因。
- 日志和指标通过独立 `TelemetryPort` 写入，业务 Service 不依赖具体日志平台。
- 关闭统计页面或更换监控适配器不影响问答、文档和认证。

本阶段已经通过独立发布审计，并作为`RAG v1.3`提交、推送和部署。任务7.8的严格线上验收以及阶段8管理员统计页面、权限、Telemetry日志和数据清理免费验收均已通过。

## 8. 阶段九：用户治理、资料审核与管理中台 `[已完成]`

### 开发前提

- 现有注册、登录、用户隔离、`user/admin`角色和管理员系统文档能力保持回归稳定。
- 当前公共知识库资料迁移时直接标记为`published`，不得重复Embedding或改变Chroma基线。
- 权限由后端数据库角色决定，前端菜单和路由守卫只负责体验。
- 本阶段先建立治理边界，再开发Agent；不能让普通用户提交的未审核文件直接进入生产检索。

### 建议拆分任务

1. **任务 9.1a `[完成]`**：通过Alembic让用户角色支持`user/admin/super_admin`，建立集中`RolePolicy/require_roles`和受控超级管理员初始化命令；只改认证权限与测试，不做页面、资料审核或部署。
2. **任务 9.1b `[完成]`**：增加角色变更审计契约与超级管理员用户管理接口；只有超级管理员可授予/撤销管理员、启用/停用账号，且不得移除最后一个超级管理员。
3. **任务 9.2 `[完成]`**：增加个人中心和“我的资料”只读基线，展示自己的账号信息、会话统计、资料提交状态与失败原因。
4. **任务 9.3 `[完成]`**：建立资料提交与审核状态机：`pending_parse`、`pending_review`、`rejected`、`indexing`、`published`、`failed`、`withdrawn`、`archived`；普通用户提交后先隔离保存和预解析，审核通过前不调用Embedding、不写生产Chroma。
5. **任务 9.4 `[完成]`**：增加管理员审核中心，支持查看原文件与解析预览、批准、拒绝和填写原因；批准后复用共享文档生命周期，失败可补偿并进入任务记录。
6. **任务 9.5 `[完成]`**：增加知识资产中心，支持筛选、标签、来源、版本、替换、下线、重新发布和失败重试；只有`published`资料可被RAG与未来Agent检索。
7. **任务 9.6 `[完成]`**：增加任务中心和审计中心。任务保存处理状态、耗时、错误摘要和受控重试；审计保存角色变更、审核、发布、下线和配置变更，不记录医学正文或密钥。
8. **任务 9.7 `[完成]`**：建设分角色工作台和统一企业级`AppShell`，普通用户看个人数据，管理员看审核/知识资产/任务/质量，超级管理员额外看用户角色和系统设置。
9. **任务 9.8 `[完成]`**：补权限矩阵、状态机、补偿清理、迁移、前端响应式和真实浏览器验收，冻结`Platform v1.4`。

### 验收

- 普通用户不能提升角色、查看他人会话或直接发布公共知识。
- 管理员不能授予管理员角色；超级管理员的角色变更可追踪且不能删除最后一个超级管理员。
- 未审核、被拒绝、失败、撤回和下线资料都不能被RAG检索。
- 审核批准、向量化或数据库写入任一步失败后，MySQL、文件和Chroma可以补偿或明确进入可重试状态。
- 用户能在“我的资料”看到自己的提交进度、拒绝原因和允许执行的操作。
- 管理页面是各模块查询结果的组合，不建立一个包含全部业务规则的万能后台Service。
- 新页面符合`docs/product-and-ui-design.md`，桌面和移动端没有遮挡、溢出或无反馈操作。

阶段九已作为`Platform v1.4`提交、推送和部署：后端权限、审核发布、知识资产、
任务审计和前端管理中台通过完整自动化、生产API黑盒和真实浏览器验收；生产迁移为
`0010`且既有27份文档只登记版本，没有重复Embedding。发布中发现的Python 3.10枚举
兼容问题已由`46e39f1`修复并补回归测试。

## 9. 阶段十：Docker 和云端部署 `[完成]`

### 建议拆分任务

1. **任务 10.1 `[完成]`**：后端和 Vue/Nginx 具备可重复构建镜像；前端使用相对 `/api/v1` 地址构建。
2. **任务 10.2 `[完成]`**：MySQL、Redis、后端、Nginx 已纳入 Compose，Chroma、上传文件、数据库和 Redis 使用独立持久卷。
3. **任务 10.3 `[完成]`**：已增加健康检查、依赖启动顺序、内存上限和秘密注入，真实 `.env` 不进入 Git 或镜像。
4. **任务 10.4 `[完成]`**：阿里云Nginx反向代理、自动备份、受控恢复、隔离恢复演练和生产备份校验通过；使用Let’s Encrypt公网IP短期证书建立受信任HTTPS、HTTP固定跳转、自动续期和纯HTTP回滚。当前没有自有品牌域名，不作虚假声明。
5. **任务 10.5 `[完成]`**：公网首页、健康、认证、会话、文档、真实上传/RAG/SSE/停止恢复、阶段8管理员统计、阶段9三层权限和管理接口、数据清理一致性已经验收；web容器和整台服务器重启后HTTPS、证书、续期timer及四类持久数据恢复通过，冻结`Cloud v2.1`。

### 验收

- 新机器按 README 可以从零启动。
- 容器重启后用户、会话、文档和向量仍存在。
- 浏览器可完成注册、登录、上传、问答、刷新恢复和查看引用。
- 服务器资源、API 费用和公网流量可观察。
- 本地开发仍在原工作区完成，服务器只接收测试通过的版本；线上数据不被代码更新覆盖。

阶段十已冻结为`Cloud v2.1`。生产使用公网IP受信任HTTPS入口；自有品牌域名和ICP备案
属于外部运营条件，不冒充已拥有能力，也不阻塞阶段十一在本地开始。完整证据见
`docs/release-audit-cloud-v2.1.md`。

## 10. 阶段十一：基于 LangGraph 的受控资料 Agent `[已完成：Agent v2.2]`

### 开发前提

- 阶段九至少完成角色、公共知识发布状态和审核边界，Agent只读取`published`资料。
- 任务7.2提供的`KnowledgeSearchPort`继续作为唯一检索入口，不复制向量检索。
- 阶段8可观测性能够记录模型和工具耗时、错误与费用。
- 普通RAG完整回归稳定，作为Agent开发的不可破坏基线。

### 建议拆分任务

1. **任务 11.1 `[已完成]`**：建立`modules/agent`骨架、LangGraph状态定义、Tool Protocol、Tool Registry和默认关闭开关，只用Mock。
2. **任务 11.2 `[已完成]`**：增加`agent_runs`、`agent_steps`、`agent_artifacts`，实现按用户隔离的Repository和状态转换。
3. **任务 11.3 `[已完成]`**：实现只读`search_knowledge`与`get_document_info`工具，只调用公开Port并只读取`published`资料。
4. **任务 11.4 `[已完成]`**：实现显式LangGraph流程：任务分类、计划、工具选择、结果检查和最终输出；最多5步，并加入超时、Token/费用预算和停止条件。
5. **任务 11.5 `[已完成]`**：增加`summarize_document`、`compare_documents`和`generate_learning_report`，输出可下载产物与来源。
6. **任务 11.6 `[已完成]`**：增加独立Agent REST/SSE接口与安全事件，不修改普通聊天SSE。
7. **任务 11.7 `[已完成]`**：增加独立`/agent`工作台、运行历史、步骤时间线、停止生成和产物展示。
8. **任务 11.8 `[已完成]`**：建立工具选择、越权、超时、预算、失败恢复和普通RAG不受影响的评估，冻结`Agent v2.2`。

### 验收

- LangChain负责模型、Prompt、工具和集成，LangGraph只负责可观察的状态图与恢复边界。
- Agent能根据任务选择正确工具，工具不能直接操作数据库、Chroma、Redis或操作系统。
- RAG与Agent是两个页面和业务链路，但共享同一套已发布公共知识。
- 运行、步骤和产物按用户隔离；数据库不保存隐藏推理过程。
- 达到步骤、时间、Token或费用上限时安全停止。
- 第一版不执行系统命令、任意Python/SQL，不做网页搜索、医疗诊断、多Agent或拖拽工作流。

## 11. 阶段十二：知识质量、记忆与文档智能 `[完成：Knowledge v2.3]`

### 建议拆分任务

1. **任务 12.1 `[完成]`**：回答点赞/点踩、问题分类、人工复核队列、聚合指标和近14天趋势已持久化。
2. **任务 12.2 `[完成]`**：最近3轮之前使用提取式滚动摘要；长期记忆默认关闭，只保存用户显式CRUD并可随时关闭。
3. **任务 12.3 `[完成]`**：PDF审核预览增加逐页文本、疑似表格、扫描/图片和解析告警；不安全解析明确失败。
4. **任务 12.4 `[完成实验闸门]`**：Docling/OCR保持可选Port候选；固定用例和严格晋级规则已建立，当前没有候选晋级，生产继续使用PyPDF基线。
5. **任务 12.5 `[完成]`**：回答来源持久化文档/切片ID，可打开PDF页码或原文，并追踪版本、来源和治理元数据。
6. **任务 12.6 `[完成]`**：知识资产支持分类、科室、失效和复核日期；到期扫描创建持久任务，完成复核同步关闭任务。

### 验收

- 质量改进有固定数据集、人工复核和趋势指标，不只展示一次好回答。
- 用户可以理解并控制记忆；删除账号或记忆后不存在隐蔽副本。
- 复杂PDF解析失败会明确提示，不会把错乱表格悄悄写入公共知识库。
- 新解析器通过Port接入，可按文档类型回退到旧实现。

## 12. 阶段十三：Codex 式对话 Agent 工作台 `[已完成]`

### 产品目标

保留阶段十一的受控LangGraph循环、工具白名单、运行记录和预算边界，在其上增加
`agent_threads/agent_messages`会话层。Agent页面升级为“聊天主界面 + 可展开计划/
工具步骤 + 历史会话 + 来源与产物”，但不展示或持久化隐藏推理。

普通RAG与Agent继续使用独立页面、接口和SSE。RAG负责快速问答，Agent负责需要规划、
多个工具步骤、连续追问和报告产物的复杂任务；两者只共享`published`公共知识。

完整产品、数据、API、SSE、界面和兼容设计见
[`docs/conversational-agent-design.md`](conversational-agent-design.md)。

### 建议拆分任务

1. **任务 13.1 `[完成]`**：新增`agent_threads/agent_messages`及`agent_runs`关联字段；通过Alembic、Repository用户隔离、级联删除和旧run兼容测试建立持久化基线。本步未改前端、未调用模型、未部署。
2. **任务 13.2 `[完成]`**：实现会话/消息服务与`AgentContextBuilder`，按预算组合当前消息、显式引用、最近消息、滚动摘要和用户启用的长期记忆。
3. **任务 13.3 `[完成]`**：增加thread/message REST和独立会话SSE，补幂等键、thread生成锁、网络断开、停止、进程重启收敛和刷新恢复契约；保留旧run API。
4. **任务 13.4 `[完成]`**：把一次用户消息与一次run关联，保存assistant message、来源和产物，支持失败/停止消息受控重试和连续任务。
5. **任务 13.5 `[完成]`**：实现三栏Codex式前端骨架：会话侧栏、消息与执行时间线、上下文/来源/产物栏，以及固定底部输入区。
6. **任务 13.6 `[完成]`**：接入计划、工具、Token、来源和产物SSE联动，支持显式选择已有消息、来源和产物继续提问。
7. **任务 13.7 `[完成]`**：验证多轮上下文、滚动摘要、用户记忆开关、旧独立run只读兼容和thread删除清理。
8. **任务 13.8 `[完成]`**：权限、状态机、幂等、预算、停止、故障、响应式和普通RAG回归通过；生产迁移与三轮真实连续报告验收完成，共3次Qwen、0次Embedding/Reranker、0次自动重试，累计费用¥0.044025。验收发现并修复安全上下文误触发拒绝及报告工具未消费会话上下文两个真实集成缺陷；临时数据精确清理、正式重试恢复2，阶段冻结为`Agent Chat v3.0`。

### 验收

- 每个用户只能查看和操作自己的Agent会话、消息、运行、步骤与产物。
- 同一会话至少支持3轮连续任务，能显式复用上一轮来源或产物。
- 页面只展示公开计划、工具状态和用户可见结果，不显示隐藏Chain-of-Thought。
- 双击发送和网络重试不会创建重复run或重复调用模型；同一thread不能并行运行两个任务。
- 刷新、重新登录、停止、失败和重试后，服务端持久状态与页面一致。
- 1280/1440桌面和390移动端不存在整页横向溢出、内容遮挡或不可达操作。
- 普通RAG、资料审核、质量反馈、记忆、知识治理和部署基线完整回归。

## 13. 阶段十四：Agent Chat v3.1 产品修复 `[已完成]`

### 发现依据

阶段13的会话、运行、步骤、来源、产物、预算和审计能力继续保留。2026-07-26复核确认：

- `agent_messages`使用`created_at + UUID`排序，同一秒创建的user/assistant在MySQL上
  可能被随机UUID交换顺序。
- 前端仍保留`thread_id=NULL`旧独立run入口和专用渲染分支。
- 固定三栏与永久“来源与产物”栏压缩正文，来源ID和英文状态信息密度过高。
- 当前路由没有“无需工具的直接回答”和“先澄清”分支，普通寒暄可能误调用知识工具。
- LangGraph实际已经支持工具循环提前结束，但界面没有表达“最多5次、实际按需”。

完整产品、数据、API、SSE、清理和验收设计见
[`docs/conversational-agent-design.md`](conversational-agent-design.md) 第16节。

### 建议拆分任务

1. **任务 14.1 `[已完成]`**：为`agent_threads`增加下一序号计数器，为
   `agent_messages`增加thread内严格递增`sequence_no`及同轮`turn_id`；基于现有run
   关系确定性回填历史，通过thread行锁一次预留user/assistant两个序号并建立唯一约束。
   消息分页、最近上下文和摘要排序统一切到sequence，增加相同时间戳、并发和真实MySQL
   契约测试。本步不改页面、不调用模型、不清理数据、不部署。
2. **任务 14.2 `[已完成]`**：删除Vue的`legacyRuns/legacySelected`及“旧版独立任务”
   入口，关闭面向用户的无thread run创建入口；增加只作用于`thread_id IS NULL`记录的
   `--dry-run/--confirm`维护脚本和隔离测试。本步不执行生产删除。
3. **任务 14.3 `[已完成]`**：把Agent页改成“左侧会话 + 中间聊天”的两栏结构，复用
   RAG聊天页的视觉节奏；过程、来源和产物内联到对应助手消息，永久右栏退出，详情改为
   按需抽屉或对话框。
4. **任务 14.4 `[已完成]`**：建立统一timeline实体/reducer，REST与SSE按
   `message_id/run_id/step_id/artifact_id`幂等归并并按`sequence_no`排序；覆盖事件重复、
   乱序、断线重连、刷新恢复和分页加载。
5. **任务 14.5 `[已完成]`**：任务首节点改为
   `direct_reply/clarification/tool_required/refuse`四类显式路由。身份、问候、反馈和
   系统边界说明为0工具回答；缺少对象时先问一个明确问题。
6. **任务 14.6 `[已完成]`**：把LangGraph过程明确为按需0～5次工具调用；模型读取公开
   工具结果后决定结束、更换工具、补充工具或澄清。保持ToolRegistry、Port、预算、停止、
   用户隔离和无隐藏推理边界。
7. **任务 14.7 `[已完成]`**：完成数据库顺序、权限、幂等、锁、状态机、工具路由、
   REST/SSE、桌面1440/1280和移动390、可访问性及普通RAG完整回归。默认使用Fake/Mock，
   不产生模型费用。
8. **任务 14.8 `[已完成]`**：发布前备份、数据库迁移、构建、部署和在线无费用
   验证。生产旧独立记录清理与真实模型场景验收必须分别获得用户当次确认；清理后证明
   threaded Agent及其他业务数据数量和引用不变。

### 阶段验收

- 会话顺序由数据库`sequence_no`保证，不依赖时间精度或UUID偶然顺序。
- 页面刷新、分页、SSE重放和网络重连后没有消息交换、重复气泡或空助手消息。
- 普通寒暄不检索知识库；复杂任务按实际需要调用0～5次工具。
- Agent页面以回答为中心，工具、来源和产物属于对应消息；没有永久空白上下文栏。
- 产品入口中没有旧版独立任务；维护脚本不会误删threaded Agent记录。
- 普通RAG、认证、资料治理、管理中台、质量、记忆和部署基线不回归。

高风险写工具的人工确认节点顺延到阶段14稳定后。本阶段不引入多Agent、任意代码执行、
网页搜索、MCP市场或可视化工作流。

## 14. 阶段十五：Agent Chat v3.2 执行时间线与界面稳定化 `[完成]`

1. **任务15.1 `[已完成]`**：在`inspect_result`后发送安全的`decision`公开事件；
   不返回或持久化隐藏推理，不修改工具白名单、预算、权限和数据库结构。
2. **任务15.2 `[已完成]`**：执行过程按“计划、选择工具、工具结果、检查结果”展示；
   运行时默认展开，结束后默认折叠，刷新后从持久step确定性重建。
3. **任务15.3 `[已完成]`**：Agent消息、输入框、来源折叠列表和会话侧栏复用RAG视觉
   语言；最终回答仍在助手气泡，来源与产物附着于对应消息。
4. **任务15.4 `[已完成]`**：删除、归档或恢复当前会话后统一清理并重建timeline、
   run详情、显式引用和详情抽屉；增加删除最后一个会话的回归测试。
5. **任务15.5 `[完成，有边界]`**：自动化回归覆盖执行时间线、来源、消息状态和删除
   最后一个会话后的状态清理；本轮浏览器控制没有建立有效页面会话，因此不虚报
   1440/1280/390线上视觉点击验收，仍由用户实际浏览器体验作为补充检查。
6. **任务15.6 `[完成]`**：发布前备份及SHA-256校验通过，只重建backend和web；
   MySQL/Redis未重建。四容器健康、迁移保持`0019`、HTTPS健康接口和HTTP 308通过，
   正式包使用同源`/api/v1`且不含本机API地址；线上Unicode零工具问候完成，临时数据
   清理为0。详细证据见`docs/release-audit-agent-chat-v3.2.md`。

本阶段不增加新Agent工具、写操作、多Agent、MCP市场或数据库迁移。

## 15. 阶段十六：邮箱账号生命周期与模型用量计量 `[已完成并发布]`

本阶段解决两个相互独立但都属于产品收尾的问题：

1. 当前注册不验证邮箱，也没有忘记密码；现有无价值测试账号需要经过外键安全清理。
2. 普通RAG模型适配器丢弃DashScope最终usage，导致管理页面的输入/输出Token和费用
   永久显示未知。

完整数据、接口、模块、前端、迁移、清理和回滚设计见
[`docs/auth-and-model-usage-design.md`](auth-and-model-usage-design.md)。

### 建议拆分任务

1. **任务16.1a `[已完成]`**：新增`email_verified_at`和
   `token_version`迁移，补SMTP/验证码配置、`EmailSenderPort`、验证码Store Port及Fake
   适配器；不改注册接口、不发真实邮件、不清理账号。
2. **任务16.1b `[已完成]`**：实现Redis验证码存储、QQ SMTP适配器、发送接口和验证码
   注册；覆盖过期、重放、并发、错误次数、枚举、限流及Redis/SMTP故障。
3. **任务16.1c `[已完成]`**：实现忘记密码请求/确认和JWT `token_version`比较，使重置
   前签发的JWT失效。
4. **任务16.1d `[已完成]`**：实现验证码注册、忘记密码和重置密码前端，保持现有登录、
   401清理、受保护路由和角色菜单不回归。
5. **任务16.1e `[已完成]`**：实现测试账号清理维护命令，先完成无副作用预检；生产
   备份、归属转移和删除必须另行获得当次授权。
6. **任务16.2a `[已完成]`**：升级普通RAG模型Port，让流式正文与最终实际usage同时
   穿透；厂商不返回计量时继续明确未知。
7. **任务16.2b `[已完成]`**：新增脱敏MySQL模型用量账本和管理员聚合，保存调用时价格
   快照并展示已知/未知调用及计量覆盖率。
8. **任务16.2c `[已完成]`**：统一Agent规划、工具后生成和最终回答的计量语义；移除
   用户可见的字符数伪计量，确定性零模型回答显示0 Token、¥0。
9. **任务16.3 `[已完成并发布]`**：L2/L3本地测试、迁移往返、多尺寸浏览器、敏感信息、
   构建检查、四类生产数据备份、配置注入、0020/0021迁移、容器重建和无费用生产验收
   已完成；真实SMTP发送、受控账号清理和真实模型计量仍必须分别取得独立确认。

### 验收

- 未取得有效邮箱验证码不能创建账号，验证码不以明文进入Redis、MySQL、日志或响应。
- 忘记密码接口不泄露邮箱是否注册；重置后旧JWT稳定返回401。
- 清理命令默认只读，未知外键或预检数量变化时停止；公共知识、文件和Chroma基线不变。
- 普通RAG和Agent在供应商返回usage时显示实际输入/输出Token；缺失时不伪造。
- 估算费用使用可配置单价快照；进程重启后历史用量仍可从MySQL账本读取。
- 认证、RAG、Agent、资料审核、角色管理、SSE停止和部署基线完整回归。

## 16. 阶段十七：自动长期记忆 v2 `[已完成并发布，自动提取开关关闭]`

本阶段升级现有`conversation_summaries/user_memory_settings/user_memories`，不新建平行
记忆系统。完整数据、Port、提取、敏感审核、相关检索、界面和回滚设计见
[`docs/memory-and-quota-design.md`](memory-and-quota-design.md)。

### 建议拆分任务

1. **任务17.1 `[已完成]`**：新增`0022_memory_v2`、Port、Repository和兼容层；把
   摘要刷新从上下文读取中拆出，旧手工记忆保持原行为。本步不改页面、不调用模型。
2. **任务17.2 `[已完成]`**：建立幂等提取任务、固定JSON契约、Fake模型、敏感策略、
   去重、冲突、有限重试和过期运行恢复。
3. **任务17.3 `[已完成]`**：完成候选确认/拒绝、修订历史、关闭保留/清空及个人中心页面。
4. **任务17.4 `[已完成]`**：实现本地相关Top-K检索与预算，普通RAG接入统一
   `MemoryContextProvider`。
5. **任务17.5 `[已完成]`**：Agent接入同一Provider；回答完成后持久化调度，SSE后台
   快速恢复与过期任务恢复完成，调度失败不改变回答终态。
6. **任务17.6 `[已完成]`**：隔离、敏感策略、Fake提取usage、完整L2、迁移、安全扫描及
   1440/1280/390浏览器验收通过；真实提取模型仍未调用。

### 验收

- 默认关闭时不提取、不注入、不调用额外模型。
- 用户只能控制自己的记忆；敏感健康信息未经确认不能生效。
- RAG和Agent使用同一相关检索入口，但按各自预算截断。
- 读取上下文不提交数据库事务，提取失败不改变原回答终态。
- 重复事件、进程中断和网络重试不产生重复候选。

## 17. 阶段十八：用户额度与用量中心 `[已完成并发布，额度执行开关关闭]`

本阶段复用阶段16的`model_usage_records`，先完成回答级计量和用户查询，再增加调用前
额度预留。完整设计同样见
[`docs/memory-and-quota-design.md`](memory-and-quota-design.md)。

### 建议拆分任务

1. **任务18.1 `[已完成]`**：把厂商无关`ModelUsage`迁入usage模块，新增回答级
   `usage_group_id`、耗时和兼容层。
2. **任务18.2 `[已完成]`**：RAG/Agent的SSE和刷新历史返回同一回答级usage；助手消息
   明确显示实际、未知、未调用和未定价。
3. **任务18.3 `[已完成]`**：用户汇总、趋势、surface/model分布、明细API和响应式个人中心完成。
4. **任务18.4 `[已完成]`**：新增套餐、分配、周期、reservation迁移、MySQL用户行/周期
   行锁和过期reservation对账；真实MySQL并发只放行一次。
5. **任务18.5 `[已完成]`**：RAG和Agent通过同一Quota Port接入，覆盖额度前置拒绝、
   actual/unknown/not_applicable、停止、失败、断开和幂等。
6. **任务18.6 `[已完成]`**：管理员总览、趋势、筛选、高消耗/异常用户、超级管理员额度
   调整与审计页面/API完成；普通管理员不能调整。
7. **任务18.7 `[已完成并发布]`**：完整回归、MySQL迁移往返、并发故障注入、SSE、
   构建、安全扫描和三宽本地浏览器通过；生产完整备份、`0021→0024`迁移、四容器、
   HTTPS和无费用黑盒通过。线上自动可视浏览器控制超时，真实模型与功能开关启用仍是
   独立闸门。

### 验收

- 助手消息刷新前后显示相同的回答级Token和估算费用。
- 两个并发请求不能共同穿透只够一次的额度，重复请求不重复扣减。
- unknown usage不伪造成实际Token；确定性零模型回答扣0。
- 用户只能查看自己的数据；管理员页面不展示问题、回答、Prompt或医学正文。
- 额度不足时不调用Qwen、Embedding或Reranker。

## 18. 阶段十九：额度策略 v2 `[本地完成，待发布预检]`

阶段18已经建立额度表、reservation、结算和页面，但生产执行开关关闭，默认计划仍为
10万Token。阶段19把默认月额度提高到100万，增加`off/shadow/enforce`、RAG动态预留、
可选费用硬限制和额度预警，同时保持系统只有普通用户、管理员、超级管理员三种角色。

完整设计见[`docs/quota-policy-v2-design.md`](quota-policy-v2-design.md)。

### 建议拆分任务

1. **任务19.1 `[已完成]`**：增加策略枚举、兼容配置和
   `0025_quota_policy_v2`；安全更新默认计划及无覆盖当前周期，不改变历史使用量。
2. **任务19.2 `[已完成]`**：实现off、shadow、enforce三级模式和脱敏would-block事件。
3. **任务19.3 `[已完成]`**：建立RAG/Agent预留估算Port，RAG不再固定预留4000。
4. **任务19.4 `[已完成]`**：完善结算、预留低估和可选费用闸门。
5. **任务19.5 `[已完成]`**：完成100万展示、80%/95%预警、实际与扣减双显示。
6. **任务19.6 `[已完成]`**：管理员只读、超级管理员调整；删除套餐输入，不增加账号身份。
7. **任务19.7 `[已完成]`**：完整本地验收与发布准备，不部署、不启用生产开关、不调用真实模型。

### 验收

- 默认用户每月100万Token、500次请求，已有使用量和人工覆盖保持不变。
- off不阻断，shadow只记录，enforce超限时模型调用为0。
- actual、unknown和not_applicable结算语义正确，并发请求不透支。
- 系统仍只有`user/admin/super_admin`三种角色。
- 管理员不能调额度，超级管理员调整不改变用户角色且写审计。
- RAG、Agent、记忆、认证和阶段18用量页面不回归。

## 19. 可选增强 `[可选]`

- Refresh Token 和主动注销。
- Qdrant/Milvus 适配器。
- 对象存储。
- CI/CD。
- 多 Agent、人工确认节点和后台任务队列。
- 可视化工作流设计器和插件市场。

这些功能只有在核心阶段稳定后再考虑，不是当前求职展示的必要条件。

## 20. 每个任务的完成模板

任务只有满足以下条件才能标记完成：

1. 代码只覆盖约定范围，没有顺手堆入下一阶段功能。
2. 新行为有自动化测试，原有回归测试通过。
3. 页面行为相关任务完成一次真实浏览器冒烟。
4. 删除/迁移任务验证目标数据和无关数据。
5. `docs/handoff.md` 记录真实结果、阅读范围和唯一下一任务。
6. 只有阶段/任务状态改变时更新本文档；架构、接口或关键策略改变时才更新 `technical-design.md`。
7. 明确模型 API 是否被真实调用以及可能产生的费用。
