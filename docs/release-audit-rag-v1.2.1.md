# RAG v1.2.1 本地发布审计

> 本文件是冻结发布与回滚证据，只在发布、部署、审计或回滚任务中读取。普通功能开发以 `docs/handoff.md` 为入口，不需要重复加载本文件或其中的报告哈希。

> 审计日期：2026-07-21
>
> 当前分支：`main`
>
> 审计基准提交：`82d1cb94bc3a11bb06ceb833cfbda747d7d1dd52`
>
> 审计完成时状态：本地发布候选验证通过；后续已完成提交、GitHub推送和受控服务器同步，详情见本文第8节及`docs/handoff.md`。

## 1. 发布决定

任务7.1～7.7形成同一个阶段七发布候选。任务7.7有效检索排序没有候选达到预登记晋级门槛，因此本次发布继续使用冻结向量基线：

```text
RAG_HYBRID_SEARCH_ENABLED=false
RAG_RERANK_ENABLED=false
```

候选池、多样性、混合检索和Reranker仅作为默认关闭、可复现的实验能力发布，不接入当前`RagService`生产主链路。没有运行后续Qwen完整回答评估，也没有修改`eval_v1`黄金标准。

## 2. 候选发布范围

审计开始时工作区包含12个已跟踪修改文件和91个未跟踪文件，共103个阶段变更文件；本审计文件是随后新增的第104个候选文件。没有已暂存内容。

应纳入后续提交的范围：

- RAG Port、当前适配器、检索策略、关键词检索、RRF融合、候选选择和Rerank基础设施。
- 默认关闭的配置项、Chroma只读评估能力以及新上传片段的可核验元数据。
- `app/evaluation/`中的版本化语料、评估Runner、预算、预检、评分、人工复核和排序实验代码。
- `backend/evaluation/`中的`corpus_v1`、`eval_v1`、JSON Schema、无正文报告、计划和审查摘要。
- 评估、RAG Port、混合检索、Reranker、候选池和真实工厂装配的测试。
- 运行与校验脚本、`.env.example`、`.gitignore`、部署说明、路线图、技术设计和handoff。
- 首次真实排序的无效报告及失效说明：它们是明确命名的审计证据，不得与有效报告混淆。

## 3. 明确排除范围

以下路径已经通过`git check-ignore`确认不会进入候选提交：

- `backend/.env`、`deploy/.env`：真实密钥与连接信息。
- `backend/chroma_db/`：本地Chroma生产数据。
- `backend/data/uploads/`、`backend/data/`：上传文件和运行时数据。
- `backend/backups/`：数据库或数据备份。
- `backend/evaluation/local_reviews/`：包含回答正文的本地人工复核包。
- `frontend/node_modules/`、`frontend/dist/`：依赖与本机构建产物。
- `.venv/`、`__pycache__/`、`.pytest_cache/`、日志和IDE配置。

本地人工复核包`human_review_capture_v1.json`仍在忽略目录内，40题绑定校验通过，到期时间为2026-07-25；到期后必须删除，不能移动到版本化目录。

## 4. 敏感信息与正文检查

- 对全部已跟踪修改和未跟踪候选文件扫描常见DashScope Key、阿里云AccessKey和私钥格式，没有发现强敏感模式。
- `.env.example`只包含`your_dashscope_api_key_here`、`change_me`和随机密钥替换说明等占位符。
- 全部版本化评估报告未出现`question`、`answer_text`、`content`、`page_content`、`body`或`prompt`正文字段。
- 报告只保存题号、类别、文档ID、指标、阶段状态和脱敏Token/费用计量。
- 候选文件最大约235KB，没有意外大型二进制、数据库、向量库或压缩备份。

## 5. 冻结与发布报告哈希

| 产物 | SHA-256 |
| --- | --- |
| `current_baseline_v1.json` | `598952a8772fde26eac428cdad0335f889241f61d12b9c55bfabed5329a26ed5` |
| `human_review_capture_v1.json` | `db4a9afc6ed4404a17512dcf5ac39017bf3fb64cf6b8da06dc874c416b6f0a84` |
| `rag_v1_2_real_comparison_v1.json` | `f54e86a1b39518d998e861c101b225b160188d373488bca4fa821913c09e2893` |
| `retrieval_ranking_mock_v1.json` | `2ca18667421937937247ebd40d502fda9d07e884a026e1795017d86e9d12f773` |
| `retrieval_ranking_real_v1.json` | `d4bdb8ad0953463c215cfa02ee08125bde4f7bc8f0ecba68eb5b036684dbd365` |
| `retrieval_ranking_real_v1_invalid_vector_port_20260721.json` | `e2396f97ca5f5f6e89c2bfb9c6e53a5e9ec064ce2135371c928a4028836acd5c` |

## 6. 发布前验证

- 后端完整测试：222项通过。
- 前端组件测试：8个文件、29项通过。
- SSE UTF-8字节分片测试：通过。
- Vite正式构建：通过。
- Python依赖一致性：`pip check`通过。
- Alembic：`0005_user_role (head)`。
- `corpus_v1`一致性：27份文档、103个Chroma片段，`passed_with_warnings`仅保留既有短片段警告。
- `eval_v1`静态校验：40题，分类14/8/6/8/4，来源覆盖27/27。
- 有效真实排序报告Schema：40题、4套候选，通过。
- MySQL只读复核：27份文档；40道评估问题在消息表匹配0条。
- `git diff --check`：通过；Windows仅提示未来检出时可能进行LF/CRLF转换，不是内容错误。

## 7. 后续操作闸门

本审计步骤本身没有执行`git add`、`git commit`、`git push`，也没有连接服务器。用户审查本清单并明确确认后，Git发布步骤才能：

1. 按本清单暂存候选文件并再次查看暂存差异。
2. 创建单一、可回滚的`RAG v1.2.1`里程碑提交。
3. 推送到GitHub并记录提交SHA。

服务器同步仍是后续独立闸门：必须先确认线上当前提交并备份MySQL、Chroma、上传文件和Redis持久数据，然后才能拉取指定提交和重建容器。不得使用`docker compose down -v`，不得在服务器直接编辑业务代码。

## 8. 实际发布与服务器同步结果

- RAG v1.2.1发布提交：`9c4cd22e84fed5b02ddffcb0c4f3e5bdc380f6a2`；上一可回滚提交：`82d1cb94bc3a11bb06ceb833cfbda747d7d1dd52`。
- 用户分别确认Git发布和服务器更新后，服务器先备份MySQL、`app_data`、`chroma_data`和Redis持久数据，四份备份的SHA-256复核均通过；备份不进入Git。
- 服务器只拉取指定提交并重建后端容器，没有执行`docker compose down -v`，没有覆盖`deploy/.env`，线上仓库保持干净。
- 更新后四个容器均健康，Alembic为`0005_user_role (head)`，Redis为`PONG`，生产开关保持`RAG_HYBRID_SEARCH_ENABLED=false`和`RAG_RERANK_ENABLED=false`。
- 免费黑盒验收通过注册、登录、当前用户、文档列表、会话创建/列表/详情/删除，临时账号已清理；公网首页、静态资源、登录路由和健康接口通过HTTP检查，未认证受保护接口返回401。
- 后端容器重启恢复通过：重启前后均为27份文档、103个Chroma片段和27个上传文件，健康接口恢复为`ok`。
- 服务器同步与免费黑盒验收阶段尚未调用真实Embedding或Qwen；后续首次付费尝试结果记录如下。自动浏览器未能加载纯IP HTTP页面，因此没有可视浏览器验收证据。

2026-07-22用户确认后执行首次付费线上验收：一个极小TXT成功上传，MySQL文档、Chroma片段和上传文件计数分别从27/103/27变为28/104/28，说明上传补偿链路前的三处写入一致。第一轮真实SSE已经返回token，但验收器只接受连续中文数字形式的预期代号，未接受可能的阿拉伯数字等价写法，因而按无重试规则在来源、done和主动停止断言前退出；本轮没有发起第二次Qwen。回答正文只存在于临时目录并已删除，不能事后推断具体表达，也不能把本轮记录为完整RAG通过。

退出清理后文档、Chroma和上传文件恢复为27/103/27，临时账号和会话删除，四个容器及健康接口正常。同期消息表新增的两条记录属于既有账号的另一个会话，不属于临时验收数据。判定器随后用固定脱敏样例增加“中文数字/阿拉伯数字/空格/Markdown”等价形式支持并通过无费用校验；未经用户重新确认，不自动调用真实Embedding或Qwen重跑。

用户再次明确确认后执行第二次尝试：上传仍使三处计数一致增加，第一轮真实SSE返回token；随后实际嵌入的解析片段因漏导入`re`在本地触发`NameError`，流程按相同停止条件退出，没有执行第二次Qwen和主动停止。补偿清理及按验收前缀扫描再次确认用户、会话、文档、文件和Chroma残留均为0，基线恢复为27/103/27，容器健康。修正后不再只测试旁路函数，而是从验收脚本提取实际解析片段，以包含token、上传来源、`done`和等价数字写法的固定SSE样例直接执行通过；该无费用结果仍不能替代第三次真实验收授权。

第三次经用户继续授权后，上传一致性再次通过；正常SSE返回2个token事件、4个来源且明确包含临时上传文档，同时存在`done`事件。模型回答没有命中预期的非医疗部署代号，事实断言失败，流程没有执行第二次Qwen和主动停止。该结果已经证明正常线上检索、引用及SSE完成事件可用，但不能证明部署代号类非医疗回答质量，也没有完成主动停止验收。退出后基线恢复为27/103/27，验收对象定向残留均为0，容器健康。

后续测试输入已在临时工具中改为可核验的医学基础事实“心脏四个腔室及名称”，实际解析片段要求数量、四个名称、上传来源和`done`同时通过，并已用完整固定SSE样例无费用执行通过；没有在第三次失败后自动发起新的付费调用。

2026-07-25进一步复核确认，线上正式SSE token事件使用`data.content`，而当时临时验收器错误读取`data.token`；因此“19个token事件但医学事实未命中”实际是验收器拼接空答案，不能据此断言模型遗漏事实。项目现新增版本化校验器并以当前字段、同义表达和缺失证据反向样例验证；临时Shell工具也修复ERR trap，服务器同一脚本失败即停自测通过。即时免费预检确认提交`70f5c5a`、四容器健康、零重试、实验开关关闭、27/103/27及4账号/5会话/40消息实时基线、验收残留0；本轮尚未产生新的模型调用或费用。

用户随后确认最多2元硬上限，最终真实验收一次完成：3次Embedding业务调用、2次Qwen SSE、0次Reranker、0次自动重试，按历史计量预计约0.01～0.03元，最终以供应商账单为准。上传使文档/Chroma/文件从27/103/27同步变为28/104/28；第一轮19个token事件、4个来源、心脏四腔室事实、临时上传来源和`done`全部通过。第二轮首个token后主动停止，73毫秒收到`stopped`，持久化状态为`completed,stopped`，生成锁释放，停止状态为`idle`，同会话幂等回放成功且没有新增模型调用。主流程清理和独立残留审计均通过，最终恢复27/103/27、4账号、5会话、40消息，五类验收残留为0，四容器健康。`RAG v1.2.1`任务7.8据此完成。
