# 当前开发交接

> 最后更新：2026-07-26
> 本文件只保留当前事实、验证边界和一个下一任务。

## 1. 新任务读取范围

完整阅读：

1. `AGENTS.md`
2. 本文件

下一任务定向阅读：

- `docs/development-roadmap.md` 的阶段10任务10.4和10.5
- `docs/technical-design.md` 第17节
- `docs/deployment.md` 的备份、恢复、重启、域名和HTTPS章节
- `docs/release-audit-rag-v1.3.md`只用于核对当前线上基线和回滚入口

## 2. 当前状态

项目是 Vue 3 + FastAPI + MySQL + Chroma + Redis + DashScope 的模块化单体。

- 阶段7已经完成：`RAG v1.2.1`已部署，最终受控线上验收通过。
- 阶段8的任务8.1～8.5已经完成，并作为`RAG v1.3`提交、推送和部署。
- 服务器运行`RAG v1.3`部署记录提交`693644e1a4ae1bdc6e86f9e46e35eaea399396ee`，四个容器健康。
- 本轮阶段8部署没有调用真实Embedding、Reranker或Qwen，没有产生模型费用；临时验收账号已删除，业务数据恢复原基线。
- 后续路线已重排：阶段9先做三层角色、资料审核、个人中心和管理中台；阶段10继续完成云端恢复；阶段11再做独立LangGraph Agent；阶段12处理质量反馈、记忆和复杂文档解析。
- 已确认目标规则：普通用户提交资料但不直接发布，管理员审核后才进入公共知识库；RAG与Agent使用独立页面并只共享`published`公共资料。
- 任务9.1a已完成：数据库角色支持`user/admin/super_admin`，集中`RolePolicy/require_roles`已建立，原管理员接口兼容`admin/super_admin`，超级管理员只能通过显式确认的幂等维护命令初始化。
- 任务9.1b已完成：超级管理员用户列表、角色与账号状态接口已建立，普通管理员不能调用；角色/状态变化实时生效并写安全审计，最后一个有效超级管理员不能停用。
- 任务9.2已完成：个人资料、个人会话/消息/资料统计和“我的资料”只读接口已建立，所有查询按当前用户隔离。
- 任务9.3已完成：普通资料进入隔离提交和无费用解析预览，旧直入Chroma上传旁路已封闭；用户可查看并撤回自己的审核前资料。
- 任务9.4已完成：管理员可查看预览、拒绝或批准；批准复用公共文档生命周期并写处理任务/审计，最终写入失败会补偿新文件、文档登记和向量。
- 阶段9审核稳定化已完成：批准/拒绝/失败重试使用原子状态抢占，知识模块通过`JobPort`维护任务；发布提交后的隔离文件清理失败只记录待清理告警，不再撤销已发布资产。
- 任务9.5已完成：知识资产支持来源/标签、版本关系、下线、重发和替换；归档向量从生产集合删除，重发失败会清理新向量。
- 任务9.6已完成：任务与审计只读接口已建立，失败发布可受控重试并保留尝试历史；普通管理员和超级管理员看到不同审计范围。
- 任务9.7已完成：统一AppShell、桌面侧栏/移动抽屉、分角色路由与菜单，以及工作台、个人中心、我的资料、管理概览、审核、资产、任务、审计和用户角色页面已接入。
- 任务9.8已完成：三层权限矩阵、审核状态机、跨存储补偿、旧数据迁移、架构边界和响应式浏览器验收通过，阶段9本地冻结为`Platform v1.4`候选。
- 任务10.4a已完成：备份/恢复脚本、校验清单、保留策略和systemd定时已建立；四类数据隔离恢复演练与生产首份自动备份通过。
- 任务10.5a已完成：整台服务器重启后四容器、HTTP健康和MySQL/文件/Chroma/Redis基线完整恢复。

## 3. 阶段7最终证据

最终受控验收实际使用3次Embedding业务调用、2次Qwen SSE、0次Reranker和0次自动重试，费用估算约0.01～0.03元：

- 临时上传后MySQL文档、Chroma片段和文件由27/103/27同步变为28/104/28。
- 短问答收到19个token事件、4个来源、正确医学事实、临时来源和`done`。
- 长问答在首个token后约73毫秒停止；状态为`completed, stopped`，生成锁释放，停止接口为`idle`，同会话幂等回放成功。
- 临时文档、会话和账号已删除；最终恢复27份文档、103个片段、27个文件、4个账号、5个会话和40条消息，五类临时残留均为0，四个容器健康。

详细发布与失败尝试历史只查`docs/release-audit-rag-v1.2.1.md`。

## 4. 阶段8实际能力

- 8.1：`TelemetryPort`、固定安全事件、JSON结构化日志和贯穿请求的`request_id`。
- 8.2：使用单调时钟记录HTTP、查询构造、向量检索、可选重排、模型生成阶段耗时，并预留工具阶段。
- 8.3：进程内聚合请求、成功/失败、耗时、限流、Redis降级、主动停止、三类基础设施失败及Token/费用状态；缺少可靠计量时明确返回未知。
- 8.4：管理员只读`GET /api/v1/admin/telemetry/stats`及独立前端统计页；普通用户稳定返回403。
- 8.5：覆盖Redis不可用、Chroma失败、模型超时、Telemetry自身失败、SSE停止、未知Token、越权和关闭Telemetry；日志按5 MiB轮转并保留5份备份。
- Docker默认将Telemetry日志写入已有`app_data`卷的`/app/data/logs/telemetry.jsonl`，可以通过环境变量关闭或调整保留上限。

Telemetry只观察业务，不记录完整问题、回答、Prompt、密码、JWT、API Key、邮箱或医学正文；关闭或写入失败都不能改变原业务结果。

## 5. 最新验证

2026-07-25发布前回归：

- 后端完整测试：250项通过。
- 前端完整测试：9个文件、30项通过。
- SSE UTF-8分片测试：通过。
- Vite正式构建：通过。
- `pip check`：无依赖冲突。
- Alembic：`0005_user_role (head)`，单头。
- Compose YAML及四项Telemetry容器配置：通过。
- 架构边界、日志脱敏、轮转/关闭和故障注入：通过。
- 新增内容敏感信息扫描和`git diff --check`：通过。

2026-07-25服务器部署与验收：

- 部署前服务器提交为`70f5c5a`，工作树干净，27份文档、103个Chroma片段、27个文件、4个账号、5个会话和40条消息。
- MySQL、`app_data`、`chroma_data`、Redis、`deploy/.env`和旧前端均已备份并通过SHA-256校验；后端和Web旧镜像保留回滚标签。
- 服务器快进到`1ca9bc0`，只重建`backend`和`web`，MySQL、Redis及命名卷没有删除或重建。
- 首次上传的前端包缺少生产`VITE_API_BASE_URL=/api/v1`，真实浏览器显示后端连接失败；验收立即捕获后重新按部署手册构建，只重建Web，最终首页显示“运行正常”。
- 未登录统计接口401、普通用户403、管理员200；登录后的`/admin/telemetry`页面真实浏览器验收通过，聚合卡片、未知Token/费用、阶段耗时和错误类型正常展示。
- Telemetry写入`/app/data/logs/telemetry.jsonl`，当前只有1个受控日志文件；临时邮箱和密码均未进入日志。
- 临时管理员账号已删除，最终恢复27/103/27、4账号/5会话/40消息，四容器健康、Redis `PONG`、服务器工作树干净。

2026-07-26任务9.1a本地验证：

- 角色策略、管理员兼容、账号停用、超级管理员初始化和迁移定向测试：19项通过。
- 后端完整测试：257项通过。
- 本步没有修改前端、资料审核、Agent、模型配置或服务器，没有调用模型。

2026-07-26任务9.1b本地验证：

- 用户治理、越权、实时授权、最后超级管理员保护、审计和迁移定向测试：22项通过。
- 后端完整测试：260项通过。
- 本步没有开发页面、资料审核、Agent，没有调用模型或修改服务器。

2026-07-26任务9.2本地验证：

- 个人中心、个人统计、资料隔离及原会话/文档接口定向测试：13项通过。
- 后端完整测试：261项通过。
- 本步没有修改上传发布链路、前端、Agent或服务器。

2026-07-26任务9.3本地验证：

- 隔离提交、解析、撤回、用户隔离、迁移、上传保护和管理员旧链路定向测试：19项通过。
- 后端完整测试：262项通过。
- 前端完整测试：9个文件、31项通过；Vite正式构建通过。
- 提交阶段没有写公共文档表或Chroma，没有调用Embedding、Reranker或Qwen。

2026-07-26任务9.4本地验证：

- 审核权限、拒绝、Fake发布、任务/审计和最终写入失败补偿定向测试：21项通过。
- 后端完整测试：264项通过。
- 只使用Fake向量库，没有调用真实Embedding或模型。

2026-07-26任务9.5本地验证：

- 资产权限、筛选、元数据、下线、Fake重发、替换、管理员旧链路和迁移定向测试：32项通过。
- 后端完整测试：265项通过。
- 没有调用真实Embedding、Reranker或Qwen。

2026-07-26任务9.6本地验证：

- 任务权限、状态筛选、审计范围、失败重试和尝试次数定向测试：11项通过。
- 后端完整测试：266项通过。
- 没有调用真实模型或修改服务器。

2026-07-26阶段9审核稳定化本地验证：

- 并发旧读、批准/拒绝互斥、发布后隔离文件清理失败、任务端口、待审核资料统计定向测试：6项通过。
- 后端完整测试：268项通过。
- 隔离文件清理失败时，发布文档、向量、完成任务和成功审计保持不变，并新增`cleanup_pending`告警审计。
- 没有调用真实Embedding、Reranker或Qwen，没有修改服务器。

2026-07-26任务9.7本地验证：

- 前端完整测试：9个文件、31项通过；SSE UTF-8分片测试通过；Vite正式构建通过。
- 真实浏览器在1440、1280和390宽视口检查工作台、用户角色表格、分角色菜单和移动抽屉；没有整页横向溢出、遮挡或控制台异常。
- 390宽表格使用局部横向滚动，移动侧栏默认收起并可通过有无障碍名称的按钮打开。
- 浏览器验收只使用临时SQLite和本地测试账号，没有调用模型、修改线上数据或部署。

2026-07-26任务9.8与阶段9最终本地验证：

- 阶段9权限、状态机、补偿、迁移和架构边界定向测试：17项通过。
- 后端完整测试：271项通过；前端完整测试：9个文件、31项通过。
- SSE UTF-8分片测试、Vite正式构建、`pip check`和`git diff --check`通过。
- Alembic为单一`0010_document_versions (head)`；从阶段9前基线升级两次仍只登记一次既有发布资料和版本，不调用Embedding。
- 敏感信息扫描只命中`.env.example`和README中的显式占位符，没有真实密钥进入新增内容。
- 阶段9没有调用真实Embedding、Reranker或Qwen，没有修改线上数据或部署；生产仍为`RAG v1.3`。

2026-07-26阶段10备份与整机恢复验证：

- 隔离Compose项目中，MySQL、`app_data`、`chroma_data`和`redis_data`经过备份、故意篡改和恢复后全部回到基线，输出`BACKUP_RESTORE_DRILL_OK`；临时容器和卷已清理。
- 生产首份自动备份目录为700、文件为600，7个清单项SHA-256全部通过；每日03:30定时器已启用，最多随机延迟10分钟并保留7份。
- 整机重启前后保持4账号、5会话、42消息、27文档、27上传文件、103个Chroma片段和2个Redis键；四容器健康、HTTP健康200。
- 以上过程调用模型0次、费用0；生产代码和业务数据未变更。

## 6. 工作区与禁止事项

- 当前分支为`main`；线上`RAG v1.3`部署记录提交为`693644e`。
- 阶段8代码、阶段7最终验收校验器、测试和文档已按`docs/release-audit-rag-v1.3.md`精确提交并推送。
- `backend/tests/test_auth_api.py`在`git status`中可能因Windows换行显示修改，但内容与HEAD一致，不属于发布范围。
- 当前工作区包含尚未提交的`Platform v1.4`应用、迁移、测试、前端和部署恢复修改；不得只挑部分业务文件发布。
- 不得清理、覆盖或回退现有修改；禁止`git reset --hard`和批量清理。
- 未经用户明确确认，不得部署或修改线上配置。

## 7. 唯一下一任务

**发布并免费验收`Platform v1.4`，不同时修改Nginx、域名或HTTPS。**

本小步边界：

- 按`docs/release-audit-platform-v1.4.md`精确提交、推送并记录候选SHA。
- 更新前重新校验生产备份和旧镜像；服务器只能快进候选提交，禁止编辑源码或删除卷。
- 只重建backend和web，让backend启动迁移到`0010`；迁移失败立即停止。
- 免费验证三层权限、管理接口/页面、既有27/103/27数据和问答SSE契约，不调用模型。
- 发布稳定后再留下域名/HTTPS这一项唯一任务。
