# 当前开发交接

> 最后更新：2026-07-27
> 本文件只保留当前事实、验证边界和一个下一任务。

## 1. 新任务读取范围

完整阅读：

1. `AGENTS.md`
2. 本文件

后续任务定向阅读：

- `docs/conversational-agent-design.md` 第17节
- `docs/development-roadmap.md` 阶段15
- `docs/technical-design.md` 第13.10节
- 新需求只读其直接相关源码、测试与设计章节

## 2. 当前状态

项目是 Vue 3 + FastAPI + MySQL + Chroma + Redis + DashScope 的模块化单体。

- 阶段7已经完成：`RAG v1.2.1`已部署，最终受控线上验收通过。
- 阶段8的任务8.1～8.5已经完成，并作为`RAG v1.3`提交、推送和部署。
- 服务器运行`Agent Chat v3.2`原生流式提交`2208815`，数据库为
  `0019_agent_message_order (head)`，四个容器健康。
- 本轮阶段8部署没有调用真实Embedding、Reranker或Qwen，没有产生模型费用；临时验收账号已删除，业务数据恢复原基线。
- 阶段9～14已经完成；阶段14已把Agent主链路升级为消息顺序确定、聊天优先和工具按需
  调用的`Agent Chat v3.1`。
- 阶段15已经完成并发布：新增安全的`decision`公开事件，执行过程按
  “计划、选择工具、工具结果、检查结果”展示并在终态折叠；Agent聊天、来源和输入区
  复用RAG视觉语言；删除当前会话会同步清空或切换右侧时间线。
- 发布前备份、容器重建、健康检查、迁移检查、生产包扫描和线上Unicode零工具问候均
  通过；浏览器控制未建立有效页面会话，因此多尺寸人工视觉观感仍是明确的补充边界。
- 零工具问答状态误判已经修复：`你是谁呢`、`介绍一下自己`等常见身份问法现在走
  确定性直接回答；失败且零步骤的历史run会显示“处理失败”，不再伪装成“直接回答”。
  线上同一临时thread验证两轮均为completed、0 step、0 Token、0估算费用，临时账号、
  thread、message、run和Redis键已清理为0。
- 模型非工具路由兼容已经修复：Qwen在direct_reply、clarification或refuse路由中返回
  `plan: null`时会规范化为空公开计划，不再触发Pydantic校验失败。线上真实模型依次
  验证“测试”“讲一下辛亥革命”“我最近一直头疼”，三轮均completed、0 step、无SSE
  error，总计987 Token、估算费用约¥0.00352；临时账号及关联数据已清理为0。
- Agent输入框聚焦交互已经补齐：页面首次加载、新建会话、切换历史会话以及模型回复
  或重试收尾后，输入框会在可用状态自动获得焦点；运行期间的disabled状态不会被绕过。
  前端41项测试、SSE解析和正式构建通过。
- 已发布候选把同一聚焦规则补到普通RAG页面，并把Agent搜索类任务的最终回答改为
  `ChatTongyi.stream -> 多个token SSE -> 单条完整消息持久化`；规划JSON提示与用户可见
  正文提示已经分离。确定性问候和安全边界说明仍允许瞬时直接回答。发布前后均未改变
  数据库迁移、知识文档、Chroma、普通RAG Prompt或检索开关。
- 已确认目标规则：普通用户提交资料但不直接发布，管理员审核后才进入公共知识库；RAG与Agent使用独立页面并只共享`published`公共资料。
- 任务9.1a已完成：数据库角色支持`user/admin/super_admin`，集中`RolePolicy/require_roles`已建立，原管理员接口兼容`admin/super_admin`，超级管理员只能通过显式确认的幂等维护命令初始化。
- 任务9.1b已完成：超级管理员用户列表、角色与账号状态接口已建立，普通管理员不能调用；角色/状态变化实时生效并写安全审计，最后一个有效超级管理员不能停用。
- 任务9.2已完成：个人资料、个人会话/消息/资料统计和“我的资料”只读接口已建立，所有查询按当前用户隔离。
- 任务9.3已完成：普通资料进入隔离提交和无费用解析预览，旧直入Chroma上传旁路已封闭；用户可查看并撤回自己的审核前资料。
- 任务9.4已完成：管理员可查看预览、拒绝或批准；批准复用公共文档生命周期并写处理任务/审计，最终写入失败会补偿新文件、文档登记和向量。
- 阶段9审核稳定化已完成：批准/拒绝/失败重试使用原子状态抢占，知识模块通过`JobPort`维护任务；发布提交后的隔离文件清理失败只记录待清理告警，不再撤销已发布资产。
- 阶段9旧系统资料删除/替换兼容已修复：替换会把兼容提交重新关联到新文档，物理删除会在同一提交中清理系统兼容提交；`0011_orphan_submission_cleanup`只清理已发布、无提交者且目标文档已不存在的历史孤儿登记，避免已删除资料继续触发上传判重。
- 任务9.5已完成：知识资产支持来源/标签、版本关系、下线、重发和替换；归档向量从生产集合删除，重发失败会清理新向量。
- 任务9.6已完成：任务与审计只读接口已建立，失败发布可受控重试并保留尝试历史；普通管理员和超级管理员看到不同审计范围。
- 任务9.7已完成：统一AppShell、桌面侧栏/移动抽屉、分角色路由与菜单，以及工作台、个人中心、我的资料、管理概览、审核、资产、任务、审计和用户角色页面已接入。
- 任务9.8已完成：三层权限矩阵、审核状态机、跨存储补偿、旧数据迁移、架构边界和响应式浏览器验收通过，阶段9已作为`Platform v1.4`发布。
- 任务10.4a已完成：备份/恢复脚本、校验清单、保留策略和systemd定时已建立；四类数据隔离恢复演练与生产首份自动备份通过。
- 任务10.5a已完成：整台服务器重启后四容器、HTTP健康和MySQL/文件/Chroma/Redis基线完整恢复。
- 阶段10已完成并冻结为`Cloud v2.1`：公网IP受信任HTTPS、HTTP 308、短期证书自动
  续期、续期热重载、HTTPS SSE、web和整机重启恢复均通过；当前没有自有品牌域名。
- 任务11.1已完成：默认关闭的Agent模块骨架、显式状态契约、Tool Protocol/Registry和最多5步及预算/超时占位边界已建立；只使用Mock，没有数据库、API、SSE、页面或模型调用。
- 任务11.2已完成：`agent_runs/agent_steps/agent_artifacts`及按用户隔离Repository、原子状态转换、步骤上限和隐藏推理字段拒绝规则已建立；当前仍没有Agent API、SSE、页面或真实执行。
- 阶段11已完成并冻结为`Agent v2.2`：五个只读白名单工具、最多5步LangGraph、
  Token/费用/超时/停止边界、用户隔离持久化、独立REST/SSE和`/agent`工作台均已建立；
  明确任务走确定性安全路由，模糊任务由LangChain规划，普通RAG链路保持独立。
- 阶段12已完成并冻结为`Knowledge v2.3`：回答反馈、问题分类、人工复核和近14天
  质量趋势已持久化；最近3轮之前使用滚动摘要，长期记忆默认关闭并由用户显式控制；
  PDF逐页解析质量、引用原文/页码/版本追溯以及分类、科室、失效和复核任务已建立。
- Docling/OCR没有通过晋级闸门，生产仍使用PyPDF；阶段12没有修改冻结检索策略、
  Prompt、Agent白名单或Chroma内容。
- `Agent v2.2 + Knowledge v2.3`已发布到生产提交`bc20151`；数据库已由`0011`
  升级到单一`0017_knowledge_governance (head)`，Agent生产策略为启用、最多5步、
  单工具30秒、整次120秒、12000 Token、单次预估费用上限¥0.05、正式重试上限2。
- 当前生产四个容器健康，HTTPS健康检查200、HTTP固定308；27份已发布文档和27个上传
  文件保持一致。2026-07-26发布前备份与当前只读检查均为100个Chroma片段；旧文档中的
  103是此前历史发布基线，本次Agent清理没有访问或改变Chroma。
- 阶段13已经完成并冻结为`Agent Chat v3.0`：保留现有受控LangGraph内核，
  新增thread/message会话层，并采用会话、消息、公开计划、工具步骤、来源和产物组合的
  Codex式工作台。
- 任务13.1已完成：`0018_agent_threads_messages`新增按用户归属的Agent thread/message，
  并给旧run增加可空的thread、触发消息和响应消息关联；旧run保持未关联且可读。Thread/
  Message Model、Schema和Repository支持用户隔离、重命名、归档、受控metadata和级联
  清理。
- 任务13.2～13.7已完成：会话/消息用例、预算化上下文、独立thread/message REST与SSE、
  消息-run-来源-产物关联、幂等与thread生成锁、网络断开/停止/失败/重试/进程重启恢复、
  三栏Agent Chat v3.0工作台、活动/归档会话切换与恢复、三类显式引用、多轮上下文、
  记忆开关、旧run兼容和删除清理均已实现并通过本地验证。
- 任务13.8已完成：数据库为`0018_agent_threads_messages (head)`；同一thread三轮真实
  报告、显式产物引用、SSE和持久化关联通过，共3次Qwen、0次Embedding/Reranker、
  0次自动重试，累计费用¥0.044025。临时数据精确清理，正式重试恢复2。
- 阶段14已经完成并冻结为`Agent Chat v3.1`：
  - `0019`增加thread内递增`sequence_no`、`turn_id`和行锁序号预留；生产10条历史消息
    已回填为1～10，唯一序号10条、空turn 0条，thread下一序号为11。
  - 前端旧独立任务入口和永久右栏已退出；REST/SSE由timeline reducer按稳定实体ID
    归并，两栏聊天页把过程、来源和产物放回对应助手消息。
  - Planner/Graph支持`direct_reply/clarification/tool_required/refuse`，工具按需
    0～5次；问候可形成0 step、0 Token、0费用run。
  - 生产发布前备份`backup-20260726T142800Z`的MySQL、app_data、Chroma、Redis及清单
    SHA-256全部通过；部署后四容器健康。
  - 线上同一临时thread按真实60秒限流分两批完成20轮问候：40条消息序号1～40、
    20个turn、20个run、0 step、0 Token、费用0，退出码0；清理后恢复5账号、1个正式
    Agent thread、10条Agent消息、11个run，临时账号残留0。
  - 用户确认删除闸门后，6个`thread_id IS NULL`旧run及6个专属step已删除；再次
    dry-run为0，threaded Agent、RAG会话、文档、文件和Chroma均未被清理脚本触及。
  - 用户确认费用闸门后，在正式重试0下完成摘要、比较、报告各1次Qwen：0次Embedding/
    Reranker、0次自动重试、12109 Token、估算¥0.04924，低于¥0.15硬上限；三个run均
    只有1个正确工具step，来源为1/2/1份，报告产生1个Markdown产物。
  - 临时账号、thread/message/run/step/artifact及Redis限流、生成锁、幂等键残留均为0，
    正式重试恢复2。验收期间原有用户新增的2轮Agent和2轮RAG对话已识别并保留。
- 长会话体验修复已经发布：RAG和Agent打开会话、切换会话及追加消息时都会直接定位到
  最新消息；Agent单次历史加载上限由50条提升为后端允许的100条，60条会话不再截断。
  本次只修改前端展示和请求上限，没有修改后端接口、数据库、RAG检索、Agent工具或模型。

## 3. 阶段7最终证据

最终受控验收实际使用3次Embedding业务调用、2次Qwen SSE、0次Reranker和0次自动重试，费用估算约0.01～0.03元：

- 临时上传后MySQL文档、Chroma片段和文件由27/103/27同步变为28/104/28。
- 短问答收到19个token事件、4个来源、正确医学事实、临时来源和`done`。
- 长问答在首个token后约73毫秒停止；状态为`completed, stopped`，生成锁释放，停止接口为`idle`，同会话幂等回放成功。
- 临时文档、会话和账号已删除；最终恢复27份文档、103个片段、27个文件、4个账号、5个会话和40条消息，五类临时残留均为0，四个容器健康。

详细发布与失败尝试历史只查`docs/release-audit-rag-v1.2.1.md`。

## 4. 阶段8实际能力

- 8.1：`TelemetryPort`、固定安全事件、JSON结构化日志和贯穿请求的`request_id`。
- 8.2：使用单调时钟记录HTTP、查询构造、向量检索、可选重排、模型生成阶段耗时，并预留工具阶段。
- 8.3：进程内聚合请求、成功/失败、耗时、限流、Redis降级、主动停止、三类基础设施失败及Token/费用状态；缺少可靠计量时明确返回未知。
- 8.4：管理员只读`GET /api/v1/admin/telemetry/stats`及独立前端统计页；普通用户稳定返回403。
- 8.5：覆盖Redis不可用、Chroma失败、模型超时、Telemetry自身失败、SSE停止、未知Token、越权和关闭Telemetry；日志按5 MiB轮转并保留5份备份。
- Docker默认将Telemetry日志写入已有`app_data`卷的`/app/data/logs/telemetry.jsonl`，可以通过环境变量关闭或调整保留上限。

Telemetry只观察业务，不记录完整问题、回答、Prompt、密码、JWT、API Key、邮箱或医学正文；关闭或写入失败都不能改变原业务结果。

## 5. 最新验证

2026-07-27 RAG聚焦与Agent原生流式候选：

- 后端完整357项测试、前端10个文件42项测试、SSE UTF-8分片、Vite正式构建和
  `git diff --check`通过。
- 假模型验收确认最终回答以2个独立`token`事件送达，运行只保存拼接后的1份完整结果，
  工具与最终生成的Token/费用正确累计。
- 受控真实模型预检限定1次Qwen、0次Embedding/Reranker、重试0、45秒停止和低于
  ¥0.01费用；实际收到7个原生模型chunk，共104 Token，估算¥0.00041。该调用只验证
  模型适配器流能力，不写MySQL、Chroma或Redis。
- 本轮没有数据库迁移、知识库变化或服务器部署；生产仍运行此前`Agent Chat v3.2`
  版本。

2026-07-27 Agent Chat v3.2发布：

- 后端完整347项测试通过；新增`decision` SSE契约测试。
- 前端10个测试文件39项通过；新增删除最后一个会话后右侧消息、run和时间线清空用例。
- Vite正式构建、SSE解析、`pip check`和`git diff --check`通过；业务提交为
  `1b6698f`并已推送。
- 发布前备份`backup-20260726T162016Z`的MySQL、app_data、Chroma、Redis、环境与
  Compose文件SHA-256全部通过；后端和Web保留`rollback-b5a37d3`镜像。
- 服务器GitHub TLS拉取失败后使用完整Git bundle做`--ff-only`更新，没有改写历史；
  只重建backend和web，MySQL/Redis持续运行。四容器健康，迁移保持`0019`，HTTPS健康
  接口200、HTTP固定308；生产资源包含`/api/v1`且不含`127.0.0.1:8000`。
- 首次Web构建误带旧`v3.1`静态目录；经用户截图发现后重新上传本地正式产物并使用
  `--no-cache`只重建Web。最终公网首页引用`index-DmAMgtUm.js`，包内标签确认是
  `AGENT CHAT V3.2`，Web恢复healthy，后端与数据服务未重建。
- 首次自动验收因Windows PowerShell把中文请求错误编码为`??`，意外进入Qwen规划
  启动路径并失败；系统记录Token 0、估算费用0，但不能据此保证供应商侧绝对无计费。
  改用Unicode JSON后，“你好”走确定性0工具路线，消息按user、assistant顺序完成，
  无error事件。
- 临时账号、3个临时thread及其message/run均清理为0。自动浏览器未建立有效页面
  会话，因此没有把多尺寸线上视觉点击写成通过。

2026-07-26长会话自动定位修复：

- 前端完整测试10个文件38项、SSE UTF-8分片、Vite正式构建和`git diff --check`通过。
- 本地临时SQLite分别构造60条RAG消息和60条Agent消息；真实浏览器打开后，RAG消息区
  `scrollTop/clientHeight/scrollHeight=6974/380/7354`，Agent为
  `8059/512/8571`，均精确位于底部；Agent实际渲染60条消息。
- 发布前备份`backup-20260726T153656Z`的MySQL、app_data、Chroma、Redis及清单
  SHA-256全部通过；只替换前端静态包并重建Web，backend/MySQL/Redis未重建。
- 生产四容器健康，HTTPS健康接口通过、HTTP固定308；生产资源
  `index-BX2mKNOd.js`与本地SHA-256一致，包含`/api/v1`且不含本机API地址。
- 本次没有调用Qwen、Embedding或Reranker，模型费用为0；没有创建或清理生产业务数据。

2026-07-26阶段14发布与无费用验收：

- 后端完整测试347项、前端10个文件38项、SSE分片、正式构建、`pip check`、
  Alembic单头和`git diff --check`全部通过。
- 本地真实浏览器通过登录、Agent两栏页、零工具问候，以及1440/1280/390宽度无整页
  横向溢出；移动端会话抽屉可打开。
- 生产迁移为`0019`，四容器健康，HTTPS健康200；正式包使用`/api/v1`且不含本地API。
- 20轮在线无费用验收与清理通过；没有调用Qwen、Embedding或Reranker。
- 两个最终闸门通过：旧独立run/step由6/6清理为0；真实验收为3次Qwen、12109 Token、
  ¥0.04924，0次Embedding/Reranker/自动重试。临时数据和Redis键清理为0，重试恢复2。
- 最终生产保持5账号、27文档、5个普通RAG会话、1个Agent thread；原有用户同期新增
  数据后为100条RAG消息、14条Agent消息、7个threaded run和6个step。Agent消息序号
  1～14唯一且连续，空turn为0，下一序号15。

2026-07-25发布前回归：

- 后端完整测试：250项通过。
- 前端完整测试：9个文件、30项通过。
- SSE UTF-8分片测试：通过。
- Vite正式构建：通过。
- `pip check`：无依赖冲突。
- Alembic：`0005_user_role (head)`，单头。
- Compose YAML及四项Telemetry容器配置：通过。
- 架构边界、日志脱敏、轮转/关闭和故障注入：通过。
- 新增内容敏感信息扫描和`git diff --check`：通过。

2026-07-25服务器部署与验收：

- 部署前服务器提交为`70f5c5a`，工作树干净，27份文档、103个Chroma片段、27个文件、4个账号、5个会话和40条消息。
- MySQL、`app_data`、`chroma_data`、Redis、`deploy/.env`和旧前端均已备份并通过SHA-256校验；后端和Web旧镜像保留回滚标签。
- 服务器快进到`1ca9bc0`，只重建`backend`和`web`，MySQL、Redis及命名卷没有删除或重建。
- 首次上传的前端包缺少生产`VITE_API_BASE_URL=/api/v1`，真实浏览器显示后端连接失败；验收立即捕获后重新按部署手册构建，只重建Web，最终首页显示“运行正常”。
- 未登录统计接口401、普通用户403、管理员200；登录后的`/admin/telemetry`页面真实浏览器验收通过，聚合卡片、未知Token/费用、阶段耗时和错误类型正常展示。
- Telemetry写入`/app/data/logs/telemetry.jsonl`，当前只有1个受控日志文件；临时邮箱和密码均未进入日志。
- 临时管理员账号已删除，最终恢复27/103/27、4账号/5会话/40消息，四容器健康、Redis `PONG`、服务器工作树干净。

2026-07-26任务9.1a本地验证：

- 角色策略、管理员兼容、账号停用、超级管理员初始化和迁移定向测试：19项通过。
- 后端完整测试：257项通过。
- 本步没有修改前端、资料审核、Agent、模型配置或服务器，没有调用模型。

2026-07-26任务9.1b本地验证：

- 用户治理、越权、实时授权、最后超级管理员保护、审计和迁移定向测试：22项通过。
- 后端完整测试：260项通过。
- 本步没有开发页面、资料审核、Agent，没有调用模型或修改服务器。

2026-07-26任务9.2本地验证：

- 个人中心、个人统计、资料隔离及原会话/文档接口定向测试：13项通过。
- 后端完整测试：261项通过。
- 本步没有修改上传发布链路、前端、Agent或服务器。

2026-07-26任务9.3本地验证：

- 隔离提交、解析、撤回、用户隔离、迁移、上传保护和管理员旧链路定向测试：19项通过。
- 后端完整测试：262项通过。
- 前端完整测试：9个文件、31项通过；Vite正式构建通过。
- 提交阶段没有写公共文档表或Chroma，没有调用Embedding、Reranker或Qwen。

2026-07-26任务9.4本地验证：

- 审核权限、拒绝、Fake发布、任务/审计和最终写入失败补偿定向测试：21项通过。
- 后端完整测试：264项通过。
- 只使用Fake向量库，没有调用真实Embedding或模型。

2026-07-26任务9.5本地验证：

- 资产权限、筛选、元数据、下线、Fake重发、替换、管理员旧链路和迁移定向测试：32项通过。
- 后端完整测试：265项通过。
- 没有调用真实Embedding、Reranker或Qwen。

2026-07-26任务9.6本地验证：

- 任务权限、状态筛选、审计范围、失败重试和尝试次数定向测试：11项通过。
- 后端完整测试：266项通过。
- 没有调用真实模型或修改服务器。

2026-07-26阶段9审核稳定化本地验证：

- 并发旧读、批准/拒绝互斥、发布后隔离文件清理失败、任务端口、待审核资料统计定向测试：6项通过。
- 后端完整测试：268项通过。
- 隔离文件清理失败时，发布文档、向量、完成任务和成功审计保持不变，并新增`cleanup_pending`告警审计。
- 没有调用真实Embedding、Reranker或Qwen，没有修改服务器。

2026-07-26任务9.7本地验证：

- 前端完整测试：9个文件、31项通过；SSE UTF-8分片测试通过；Vite正式构建通过。
- 真实浏览器在1440、1280和390宽视口检查工作台、用户角色表格、分角色菜单和移动抽屉；没有整页横向溢出、遮挡或控制台异常。
- 390宽表格使用局部横向滚动，移动侧栏默认收起并可通过有无障碍名称的按钮打开。
- 浏览器验收只使用临时SQLite和本地测试账号，没有调用模型、修改线上数据或部署。

2026-07-26任务9.8与阶段9最终本地验证：

- 阶段9权限、状态机、补偿、迁移和架构边界定向测试：17项通过。
- 后端完整测试：271项通过；前端完整测试：9个文件、31项通过。
- SSE UTF-8分片测试、Vite正式构建、`pip check`和`git diff --check`通过。
- Alembic为单一`0010_document_versions (head)`；从阶段9前基线升级两次仍只登记一次既有发布资料和版本，不调用Embedding。
- 敏感信息扫描只命中`.env.example`和README中的显式占位符，没有真实密钥进入新增内容。
- 阶段9没有调用真实Embedding、Reranker或Qwen，没有修改线上数据或部署；生产仍为`RAG v1.3`。

2026-07-26阶段10备份与整机恢复验证：

- 隔离Compose项目中，MySQL、`app_data`、`chroma_data`和`redis_data`经过备份、故意篡改和恢复后全部回到基线，输出`BACKUP_RESTORE_DRILL_OK`；临时容器和卷已清理。
- 生产首份自动备份目录为700、文件为600，7个清单项SHA-256全部通过；每日03:30定时器已启用，最多随机延迟10分钟并保留7份。
- 整机重启前后保持4账号、5会话、42消息、27文档、27上传文件、103个Chroma片段和2个Redis键；四容器健康、HTTP健康200。
- 以上过程调用模型0次、费用0；生产代码和业务数据未变更。

2026-07-26 `Platform v1.4`生产发布与验收：

- 发布提交`1010c68`和Python 3.10兼容修复`46e39f1`已推送并通过校验Git bundle快进到服务器；生产未直接编辑源码。
- 首次镜像在迁移后暴露`enum.StrEnum`不兼容并停止健康检查；改用已有兼容层、补架构测试后，后端完整测试276项通过并恢复部署。
- Alembic为`0010_document_versions (head)`；27份既有文档、27份published submission、27个版本和103个Chroma片段一致，没有重复Embedding。
- 普通用户个人接口200、管理接口403、旧直入文档POST为405；super_admin的审核、资产、任务、审计、Telemetry和用户治理接口全部200。
- 生产首页真实浏览器显示标题与后端运行正常；生产登录表单的后续自动定位因浏览器插件超时，角色页面以同哈希构建的本地1440/1280/390真实浏览器结果和生产API黑盒共同验收。
- 费用预检后使用0重试执行1次`text-embedding-v4`和1次`qwen3-max`，0次Reranker，返回19个token事件、119字和4个来源；来源主题偏泛，不改变冻结向量基线策略。
- 临时账号和限流键已清理；最终为4账号、5会话、42消息、27文档、27 submission、27版本、103片段、0任务、0审计和2个原Redis键，四容器健康。

2026-07-26 `Cloud v2.1` HTTPS与恢复验收：

- HTTPS实现提交`b323d18`至`21cda0e`已推送并通过校验Git bundle快进服务器；完整后端280项、前端31项、SSE和构建通过。
- 新全量备份`backup-20260725T182511Z`完成；HTTP配置备份
  `/home/deploy/medical-rag-config-backups/https-20260726T023000Z`的5项SHA-256通过。
- 未备案sslip临时域名的多视角验证失败后没有无限重试；使用Certbot 5.7和Let’s Encrypt短期配置为公网IP`112.124.9.120`签发受信任证书。
- 证书SAN精确匹配公网IP、链验证OK、HTTP固定308跳转、外部443和HTTPS健康200；Snap续期timer enabled/active，dry-run和Nginx热重载通过。
- HTTPS真实SSE使用1次Embedding和1次Qwen、0次重试、0次Reranker，收到13个token事件、4个来源、`done=1`和`error=0`。
- 临时账号及3个限流键清理后，web和整机重启均保持4账号、5会话、42消息、27文档、27 submission、27版本、103片段和2个Redis键，四容器healthy。
- 真实浏览器打开`https://112.124.9.120/`显示“运行正常”且没有证书警告；公网IP是可信演示入口，但不是自有品牌域名。

2026-07-26 生产账号权限维护：

- 按用户明确请求，通过显式确认的维护命令把一个已存在、已启用的管理员账号提升为`super_admin`；独立回查确认目标角色即时生效，当前有效超级管理员为1个。未修改密码、创建账号、部署代码或调用模型，交接文档不记录账号邮箱。

2026-07-26 系统资料删除判重修复本地验证：

- 生产只读核查确认目标系统资料的文档登记和向量已经删除，但遗留1条`published`、无提交者且`document_id`为空的兼容提交，哈希判重因此误拦普通用户重新提交。
- 系统资料替换、删除、相同内容重新提交和`0011`精确清理定向测试通过；完整后端282项通过，新增迁移revision长度兼容测试，`pip check`、单一Alembic head及`git diff --check`通过。
- 运行统计的Token与费用`unknown/null`符合阶段8技术设计：当前生产模型适配器没有提供可靠计量时不伪造数据。本次没有修改计量策略，也没有调用模型。

2026-07-26 系统资料删除判重修复生产发布：

- 发布前生产为`41a0228`、26文档、27提交和1条目标系统孤儿登记；全量备份`backup-20260726T025026Z`的7项SHA-256、目录700和文件600全部通过。
- 首次迁移正文开始执行后，过长revision无法写入MySQL的Alembic `version_num`并使后端停止健康；事务回滚后仍为`0010`且目标孤儿仍为1，没有继续验收。revision缩短并增加长度回归测试后，以提交`e5f02bb`重新构建后端成功。
- 最终Alembic为`0011_orphan_submission_cleanup`，26文档、26提交、目标孤儿0；目标文件原SHA-256在文档和提交表中的匹配均为0，判重预检通过。四容器healthy、HTTPS健康200、服务器工作树干净。
- 本次只重建后端，没有重建Web、MySQL、Redis或删除命名卷；没有调用Embedding、Qwen或Reranker。

2026-07-26 任务11.1本地验证：

- Agent默认关闭、状态/预算契约、工具白名单与参数校验、Agent不导入基础设施及普通RAG不依赖Agent定向测试16项通过。
- 完整后端287项通过；没有新增数据库表、API、SSE或页面，没有安装LangGraph运行依赖或调用模型。

2026-07-26 任务11.2本地验证：

- Agent迁移、Repository用户隔离、运行状态机、步骤上限、产物归属和隐藏推理字段拒绝定向测试14项通过。
- 完整后端289项通过；Alembic新增单头`0012_agent_persistence`，没有新增API、SSE、页面或调用模型。

2026-07-26 阶段11最终验证：

- 后端完整测试311项、前端10个文件33项、独立SSE、普通聊天SSE和Vite正式构建通过；
  Alembic单一`0012_agent_persistence (head)`，本地MySQL由0005按迁移升级到0012。
- 固定6案例真实规划评估最终6/6通过；首轮5/6后只复测报告选择分歧，固定评估已知
  计量合计5864 Token、约¥0.01856。
- 真实完整报告验收使用`26_住院手术检查与患者安全.txt`：1步
  `generate_learning_report`、1个Markdown产物、来源ID正确、2594 Token、
  约¥0.015485，低于单运行¥0.05上限。
- 调试期间5次因PowerShell中文管道或15秒超时形成的无效完整验收已停止并清理；
  其中有计量的4次合计约¥0.00957，另1次超时未返回可靠usage。最终临时用户和运行均为0，
  文档保持27、Chroma保持103片段；没有写入或删除知识资产。

2026-07-26 阶段12最终本地验证：

- 阶段12完成时后端完整测试320项；部署配置修复后最终完整测试321项。前端10个文件
  33项、普通聊天SSE和Vite正式构建通过；
  Python编译、`pip check`和`git diff --check`通过。
- SQLite完整迁移测试通过；真实MySQL首次在`0015`发现旧版本不允许JSON默认值后安全
  停在`0014`，迁移改为先加可空列、回填空JSON再设非空，随后成功升级到单一
  `0017_knowledge_governance (head)`。
- MySQL升级前后文档/已发布文档/提交保持27/27/27；固定语料清单为27份文档、
  103个Chroma片段，状态`passed_with_warnings`。
- 1280真实浏览器检查个人记忆、问答反馈、引用追溯、质量趋势/复核、解析审核、
  知识治理和任务中心，整页横向溢出为0且控制台无错误；本轮浏览器运行面板不能动态
  切换390，因此没有把390写成真实已测证据。
- 浏览器临时账号、会话、反馈和记忆已删除；阶段12模型调用0次、费用0，文档和Chroma
  未写入或删除。

2026-07-26 `Agent v2.2 + Knowledge v2.3`生产发布与验收：

- 发布前生产提交`e5f02bb`、数据库`0011`、27/103/27知识基线和工作树已核对；
  全量备份`/home/deploy/medical-rag-backups/backup-20260726T044708Z`的7项SHA-256
  全部通过，旧前端及部署配置另有可恢复副本。
- 首次快进遇到历史遗留的root属主源码文件，确认HEAD未移动后只修正精确目标属主并恢复
  失败合并产生的文件，再用已校验Git bundle快进；前端旧产物也只修正精确目录属主后替换。
  应用最终为`bc20151`，Compose显式把Agent策略传入后端容器。
- Alembic从`0011`成功升级到单一`0017_knowledge_governance (head)`；公网HTTPS黑盒
  通过记忆开关/CRUD、回答反馈、质量聚合与复核、引用追溯与原文预览、治理扫描、
  Agent历史和健康检查。
- 付费前预检限定1次完整报告、0次Embedding、0次Reranker、0次自动重试、¥0.05和
  120秒停止条件。实际运行`65ed1e8f-c40a-480e-ac88-c7e2083d9610`完成1步，
  生成1个Markdown产物，来源文档正确，共2577 Token、预估¥0.0154425。
- 生产真实浏览器在1280宽检查Agent、个人中心和回答质量页，在390×844检查同三页；
  均无整页横向溢出，移动导航可达，页面控制台无错误。
- 两个验收账号及其会话、反馈、记忆、Agent运行、步骤和产物均已清理；正式模型重试上限
  已恢复为2。最终为5个既有账号、5个会话、76条消息、27/27/27知识登记、
  103个Chroma片段、27个文件和18个既有会话幂等键；验收期间其他账号产生的3次Agent
  运行按用户数据边界保留，未被清理。

2026-07-26任务13.1本地验证：

- Agent Thread/Message迁移、用户隔离、重命名、归档、metadata白名单、跨用户拒绝、
  级联删除和旧run兼容定向测试：18项通过。
- 后端完整测试：326项通过；Python编译通过；MySQL离线迁移SQL从`0017`生成到单一
  `0018_agent_threads_messages (head)`。
- 本步没有修改API、SSE、前端、LangGraph、Prompt、工具白名单、RAG、知识和记忆行为，
  没有调用Embedding、Reranker或Qwen，没有产生模型费用，也没有修改服务器。

2026-07-26任务13.2～13.8最终验证：

- Agent上下文、三轮连续任务、显式消息/来源/产物、滚动摘要、记忆默认关闭/显式启用、
  thread/message/run用户隔离、级联清理、旧run兼容、幂等回放、thread锁、网络断开、
  停止、失败重试和进程重启收敛定向测试31项通过。
- 最终后端完整测试340项通过；前端10个文件36项通过；普通SSE UTF-8解析、Vite正式构建、
  Python编译、`pip check`通过。生产构建使用`/api/v1`且不包含本机API地址。
- Alembic单一head为`0018_agent_threads_messages`；本地MySQL当前仍为`0017`，迁移SQL、
  SQLite完整迁移及回滚测试通过，真实生产MySQL迁移留在已备份的发布步骤。
- 1440、1280和390真实浏览器检查三栏工作台、移动会话/上下文抽屉、消息/来源/产物
  三类显式引用；整页横向溢出为0，控制台无错误。临时SQLite、账号和进程已清理。
- 本地验证只使用Fake Planner和Mock工具，Embedding、Reranker、Qwen调用0次、费用0；
  未修改普通RAG Prompt、检索开关、知识文档或Chroma。
- 生产预检确认提交`97a9d10e17ee`、数据库`0017`、四容器健康、HTTPS健康200和干净工作树；
  发布前备份`backup-20260726T084026Z`的7项SHA-256全部通过，权限为目录700/文件600。
- 候选行为基线`6f5e7a8`已推送并部署，生产迁移为`0018 (head)`，四容器健康，HTTPS 200、
  HTTP 308；显式产物引用会携带当前用户产物最多1200字符的受预算内容摘录。
- 无费用生产黑盒覆盖认证、thread创建/列表/空消息、跨用户404、重命名、归档、删除、
  未认证401和旧run空列表；一次性账号、thread均精确清理为0，没有调用模型。
- 发布后文档/提交/版本仍为27/27/27，Chroma仍为103，旧Agent run/step/artifact仍为
  6/6/0，新agent thread/message为0。普通RAG消息由线上现有用户在发布期间从92增加到94，
  属于独立实时业务流量，未被候选发布修改或回滚。
- 用户当次确认后，生产自动重试临时改为0。同一thread三轮
  `generate_learning_report`分别使用2362、2361、3245 Token，费用¥0.014905、
  ¥0.0149025、¥0.0142175，累计7968 Token、¥0.044025；Qwen共3次，
  Embedding/Reranker/自动重试均为0。
- 真实验收先发现系统安全说明误触发确定性拒绝；该次为0 step/0 Token/¥0且未调用模型。
  `05872fe`把确定性路由限定到当前用户任务。随后发现报告工具没有消费完整会话上下文，
  `bee1ee7`通过`AgentToolContext.task_context`把最近消息和显式产物摘录传给生成Port。
  两项缺陷都在继续付费调用前通过定向与全量测试、提交、推送和生产部署。
- 最终同一临时thread持久化6条message、3个run、3个step和3个Markdown artifact；
  第三轮用户消息显式关联第二轮产物，SSE、来源、响应消息和终态一致。验收账号及全部
  thread/message/run/step/artifact、3个精确幂等键、限流键和生成锁清理为0。
- 正式重试恢复2；最终生产为5个既有账号、5个普通RAG会话、94条普通RAG消息、
  27/27/27文档/提交/版本、103个Chroma片段、27个上传文件、6/6/0个既有旧
  run/step/artifact和0个新thread/message。四容器健康，HTTPS 200、HTTP 308。
- 受控浏览器可加载生产登录页，但没有发出登录请求；Windows界面兜底因无法可靠确认
  URL而安全中止。没有伪报生产UI自动化通过；本地1440/1280/390真实浏览器、生产静态
  资源哈希与生产REST/SSE黑盒共同作为界面和链路证据。

2026-07-26阶段13生产登录回归修复：

- 用户报告登录页显示“无法连接后端”后，公网健康检查200、登录接口可达和容器日志确认
  后端正常；生产Web实际静态包仍包含`http://127.0.0.1:8000/api/v1`，浏览器因此错误
  访问使用者本机。此前“生产构建不含本机API地址”的最终证据已被当前生产包反证。
- `beabb3b`把前端API默认值改为同源`/api/v1`并增加回归测试；前端10个文件37项通过，
  正式构建通过，产物包含`/api/v1`且不包含`127.0.0.1:8000`。
- 只重建并替换Web容器，旧前端保存在服务器仓库外的精确备份目录；后端、MySQL、Redis、
  Chroma、数据库迁移、模型策略和业务数据均未修改。生产四容器健康，公网HTTPS健康200。
- 公网实际下载资源为`index-BptCeQ5I.js`，确认不含本机API地址；一次性账号完成
  注册、登录和`/auth/me`三步黑盒且身份一致，两个回归账号均精确清理，专用邮箱前缀
  残留为0。本轮模型调用0次、费用0。
- Chrome和内置浏览器的自动页面打开均超时，因此没有把自动点击登录写成通过；公网
  静态资源与真实认证黑盒已确认原始阻断原因消除，仍需用户浏览器强制刷新后验证界面。

## 6. 工作区与禁止事项

- 当前分支为`main`；Agent原生流式业务提交`2208815`已推送并部署，生产数据库
  保持`0019`。
- 生产源码工作树干净，四个容器健康；全量备份
  `backup-20260726T174443Z`、发布前前端目录和
  `rollback-5e394b10-20260727T014452`镜像保留回滚路径。
- 阶段8代码、阶段7最终验收校验器、测试和文档已按`docs/release-audit-rag-v1.3.md`精确提交并推送。
- `backend/tests/test_auth_api.py`在`git status`中可能因Windows换行显示修改，但内容与HEAD一致，不属于发布范围。
- `Platform v1.4`应用、迁移、测试、前端和部署恢复修改已经整体发布，后续文档提交不得夹带业务行为变化。
- 不得清理、覆盖或回退现有修改；禁止`git reset --hard`和批量清理。
- 后续付费模型验证仍需遵守新的独立预算闸门；阶段14的本次授权已经执行完毕，不能
  自动沿用到下一任务。

## 7. 唯一下一任务

**用户浏览器确认RAG与Agent输入框聚焦；如需验证公网Agent原生流，再单独授权一次受控模型验收。**

本小步边界：

- 免费发布验收已经确认四容器健康、迁移head、HTTPS主页与健康接口200、HTTP 308，
  线上静态包哈希与本地候选一致且不含本机API地址。
- 输入框焦点属于浏览器DOM交互，仍由用户在现有登录会话中确认RAG和Agent各一次。
- 真实验收如获新授权，限定1次Qwen、0次Embedding/Reranker、重试0和费用/超时/清理上限，确认
  公网SSE在最终完成前已收到多个token事件，随后清理临时账号、thread、message和run。
