# Medical RAG Design System

## Direction

登录后的产品是 `Operate` 模式的企业医疗知识工作台；未登录系统概览是独立的 `Persuade` 展示面。工作台采用轻量 macOS 生产力工具语言：浅灰蓝画布、半透明玻璃外壳、清晰正文面板、蓝紫操作色和少量语义状态色。玻璃用于建立空间层级，不能削弱聊天、表格和医学资料的可读性。

设计参数：变化度 `5`，动效 `3`，信息密度 `7`。

## Layout

- 桌面应用壳：`clamp(240px, 17vw, 300px) minmax(0, 1fr)`，外边距与区块间距为 `18px`。
- 侧栏、顶栏是独立玻璃容器；内容区自己滚动，聊天页只滚动会话和消息区域。
- 顶栏高度约 `76px`；普通页面标题容器约 `104px`，标题字号控制在 `24px` 左右。
- 桌面 RAG/Agent 不重复显示页面标题卡，聊天面板直接位于顶栏下方；移动端保留紧凑标题和会话入口。
- 聊天消息正文约 `13px / 1.62`，输入器一行起步并随内容增长，最高四行。
- 内容使用 12 列思想组织，一主多辅；数据列表通常采用 `2fr / 1fr`。
- 1024px 及以下使用抽屉导航；390px 使用单列重排，不删除字段或操作。

## Tokens

- 画布：`#F6F7F9`、`#EEF1F5`、`#F5F1F7` 与大面积柔和径向光照。
- 文字：主文字 `#1D1D1F`，正文 `#465166`，次文字 `#6E6E73`，辅助 `#8A8F98`。
- 品牌：`#5E7BFF -> #7A5CFF`，仅用于主操作、选中态和少量关键强调。
- 状态：成功 `#3A9B72`，警告 `#D98D34`，危险 `#D75B61`。
- 玻璃：`rgba(255,255,255,.62)`，白色半透明描边，`blur(24px) saturate(180%)`。
- 圆角：应用壳 `30px`，功能卡 `22px`，内层 `16px`，控件 `13px`，胶囊 `999px`。
- 字体：Inter、SF Pro、系统中文字体；字间距固定为 `0`。

权威实现位于 `frontend/src/style.css`，组件只在业务确有特殊布局时保留局部样式。

## Hierarchy

1. 页面主状态或主要任务是唯一视觉主角。
2. 2 至 4 个辅助指标支撑判断，视觉重量低于主状态。
3. 列表、详情、聊天、任务和配置承载主要操作。
4. 来源、时间、技术错误和帮助按需展开。

不得伪造图表、评分、SLA、客户数或诊断指标。卡片只用于独立业务面板，内层内容优先使用轻量行和留白，不继续嵌套同权重卡片。

## Components

- `AppShell`：浮动玻璃侧栏、顶栏、账号菜单与独立内容滚动区。
- `Public Overview`：长滚动产品概览，允许大标题、技术生态轨道、滚动揭示和真实能力计数；不得伪造客户、SLA、认证或业务指标。
- `Public Navigation`：桌面端同时提供系统概览、API中转站和登录入口；390px下中转站收敛为带无障碍名称的外链图标按钮。
- 主按钮：44px 以上蓝紫渐变胶囊；次按钮为半透明白底细描边。
- 指标：桌面一张主指标卡加辅助卡；数字使用 tabular figures。
- 表格：浅色表头、轻分割线、局部横向滚动；状态必须同时显示文字和颜色。
- RAG/Agent：共享会话侧栏、居中消息正文、固定输入器、来源和用量视觉语言。
- `ModelSelector`：位于RAG/Agent输入器内；当前模型与单价可见，未接入模型保留禁用态，不允许伪装成可切换。
- `UsageMeta`：分开显示模型实际Token、输入/输出、额度扣减和估算费用；未知计量与未配置单价不得显示为0。
- `Knowledge Assets`：用户提交与系统资料统一进入同一列表，通过来源、状态、标签和治理信息筛选；系统资料的上传、替换与永久删除仍受管理员权限保护。
- Agent 执行过程：运行时展开，结束后折叠，只展示公开决策和工具事件，不展示隐藏推理。
- 弹窗/抽屉：强玻璃材质，危险操作仍使用明确语义色和确认步骤。

## Motion And States

- 卡片 hover 上浮 `2px / 180ms`，标签与按钮上浮 `1px / 160ms`。
- 页面首次进入使用 `8px` 位移与 `220ms` 淡入。
- 公共概览的内容区按滚动位置分段进入；技术生态轨道持续横向移动，悬停时暂停且当前条目轻微放大、加深；能力计数首次进入视口时在约 `1.5s` 内增长到真实目标值。
- 首页关键标题和公共登录主按钮允许使用持续流动的蓝、青、紫渐变，作为用户明确指定的展示页品牌动效；工作台正文和管理表格不得扩散使用。
- 必须实现 default、hover、focus、active、disabled、loading、success 和 error。
- `prefers-reduced-motion` 下将动画压缩到 `1ms`。

## Boundaries

- 不复制新星数科或 SDWAN-GEO 的 Logo、品牌、文案、指标和源码。
- 不为了视觉重构改变 FastAPI 接口、权限、SSE、RAG、Agent、记忆、额度和数据库语义。
- 不引入第二套 UI 框架、Tailwind、额外状态管理或假数据。
- 管理页保持紧凑可扫描；聊天正文保持高对比；移动端不出现页面级横向滚动。

## Verification

视觉发布前至少检查 `1440x900`、`1280x800`、`1024x768`、`390x844`，验证文本遮挡、页面溢出、独立滚动区、固定输入器、抽屉、弹窗、控制台错误和所有核心操作。
