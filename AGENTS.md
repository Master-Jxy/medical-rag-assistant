# Codex Project Guidance

## 1. 项目定位

本项目是用于求职展示和持续学习的医疗知识库智能问答系统。当前已经具备 Vue 3 + FastAPI + MySQL + Chroma 的完整 RAG 主链路、认证与管理员能力、Redis 接口保护、RAG 评估、可观测性和 Docker 云端运行基线；后续按企业应用思路补齐用户治理、资料审核、管理中台、受控 Agent 和部署恢复能力。

项目采用**模块化单体**，不是微服务。目标是让每个业务模块职责清晰、可以独立测试和替换，同时保持部署简单，适配当前 2 核 2G 的演示服务器。

> 项目仅用于学习和医学资料检索，不提供临床诊断、处方或治疗建议。

## 2. 新任务开始前的读取顺序

每个新对话只需要完整阅读两份短入口：

1. `AGENTS.md`：长期稳定的开发规则和模块边界。
2. `docs/handoff.md`：当前真实状态、工作区提醒和唯一下一任务。

然后按 `docs/handoff.md` 的“本任务阅读范围”定向读取：

3. `docs/development-roadmap.md` 只读当前阶段，不从头回放已完成阶段。
4. `docs/technical-design.md` 只读当前模块对应章节；除架构审计外禁止把全文作为每步必读材料。
5. 页面、权限或交互任务再读取 `docs/product-and-ui-design.md` 的对应章节；纯后端任务不全文加载界面规范。
6. 只读与本任务直接相关的源码、测试和摘要。大型 JSON 评估报告不得整份加载，优先读取 `backend/evaluation/reviews/*_summary.md`，需要具体字段时用脚本或结构化查询提取。

发布、部署或回滚任务才读取 `docs/release-audit-rag-v1.2.1.md` 和 `docs/deployment.md`；普通业务开发不重复读取历史发布证据。

`reference/original-rag-agent/` 只在需要理解旧课程项目时阅读，不允许直接复制或改写其中的数据、密钥和环境。

文档发生冲突时，优先级为：**当前代码与测试 > handoff > roadmap > technical-design > README**。发现冲突必须在本次任务中修正文档。

## 3. 当前阶段

稳定能力包括：Vue/FastAPI/MySQL/Chroma RAG 主链路、认证与用户隔离、Redis接口保护、
SSE停止与幂等、RAG评估、可观测性、三层角色、资料审核、管理中台、受控LangGraph
Agent、知识质量、可控记忆、文档治理和Docker/HTTPS云端基线。阶段7～12均已发布；
任务7.7没有候选达到晋级门槛，生产继续使用冻结向量基线，混合检索和Reranker默认关闭。
阶段13的Agent Chat v3.0已经上线，但2026-07-26代码与产品审计确认消息排序、旧独立run
入口、固定三栏和无需工具消息误路由需要在阶段14修复；不得把v3.1目标提前写成现状。

当前准确状态、数据数量、线上验收缺口和唯一下一任务只在 `docs/handoff.md` 维护，不在本文件复制。数据库迁移、发布提交和历史验证证据分别以代码/Alembic、Git记录和发布审计为准。

已经确认的产品决策：

- 使用邮箱和密码开放注册、登录。
- 每个账号只能查看和操作自己的聊天会话。
- RAG 问答和 Agent 是两个独立页面与业务模块，但只检索同一套状态为 `published` 的公共知识库。
- Agent页面目标是Codex式对话工作台：会话历史承载连续交互，run/step/artifact继续承载受控执行、过程和产物；不展示或持久化隐藏推理。
- 用户角色演进为 `user`、`admin`、`super_admin`；只有超级管理员能授予或撤销管理员角色。
- 普通用户可以提交资料并在“我的资料”查看进度，但不能直接发布到公共知识库；管理员审核通过后才解析、向量化并公开检索。
- 普通用户可在审核前撤回自己的提交；已发布资料由管理员负责下线、替换和版本管理。
- 管理员负责资料审核、知识资产和处理任务；超级管理员额外负责用户、角色和系统级设置。
- 已有系统资料迁移后视为 `published`，不得因新审核流程重复向量化。
- 基础登录版本使用短期 Bearer JWT，不做邮箱验证码、找回密码和刷新令牌。
- 权限真相源始终是数据库；前端隐藏菜单只改善体验，不能代替后端授权。
- 管理员直接维护系统资料时可以跳过小时频率额度，但必须继续复用上传并发租约、文件校验、去重和文档生命周期；禁止在路由或 Redis 适配器里新增角色判断。

Redis、SSE、RAG Port和管理员边界的实现细节在技术设计对应章节维护。任何真实 Embedding、Reranker、Qwen 或付费线上验收都必须先完成无费用预检，说明调用次数、预计费用、停止条件，再由用户在当次对话明确确认。

## 4. 模块边界

后端调用方向固定为：

~~~text
API 路由 -> 应用服务 -> Repository / Infrastructure -> MySQL / Chroma / Redis / 模型服务
~~~

必须遵守：

- `api` 只处理参数、身份依赖和响应，不直接写 SQL、Chroma、Redis 或调用模型。
- `service` 负责一个业务用例的编排，不读取 FastAPI Request，不拼接前端展示结构。
- `repository` 只负责 MySQL 查询和持久化，不调用模型或向量库。
- `infrastructure` 封装外部系统，例如 Chroma、DashScope、Redis 和文件存储。
- Pydantic Schema 是 API 契约；SQLAlchemy Model 不直接作为跨模块输入输出。
- `auth` 拥有账号、角色和权限策略；`knowledge` 拥有资料提交、审核状态、版本与发布生命周期。
- `jobs` 只拥有可持久化的处理任务状态、重试和错误摘要；`audit` 只记录关键管理操作，不承载业务判断。
- 管理后台只聚合各模块提供的查询结果，不能成为包含用户、知识库、任务和 Agent 规则的“万能 Service”。
- RAG 检索和回答生成逐步拆开；会话服务只负责组织流程，不实现检索算法。
- 跨模块复用必须经过小而稳定的 Port/Protocol 或公开应用服务，禁止导入其他模块的 Router、Repository 和私有实现。
- Agent 是独立业务模块。LangChain 继续负责模型、Prompt、工具与外部集成，LangGraph 只负责显式状态流转和工具调度；工具只能调用公开应用服务，禁止直接查询数据库、操作 Chroma 或访问系统命令。
- Agent会话、消息、运行、步骤和产物必须分层；一条用户任务消息至多触发一个run，禁止把多轮上下文拼接、工具执行和页面状态继续堆入现有`AgentView.vue`或单个万能Service。
- 普通 RAG 和 Agent 使用独立路由、用例和 SSE 事件；Agent 故障不得改变现有聊天接口契约。
- Redis 通过限流和锁适配器提供能力，业务模块不接触 redis-py 客户端，也不能把业务主数据只放在 Redis。
- 通用代码只放配置、异常、日志、安全和请求标识；不要建立包含业务逻辑的“万能工具类”。

禁止出现：

- 路由文件里出现复杂业务判断。
- Vue 页面直接调用模型、MySQL、Chroma 或拼接密钥。
- 一个服务同时负责认证、文档、会话、检索和 Agent。
- Agent 工具绕过应用服务，直接调用 Repository、SQLAlchemy Session 或第三方 SDK。
- 普通用户提交文件后绕过审核直接写入生产 Chroma。
- 只在 Vue 路由或按钮层判断角色，却没有后端权限依赖。
- 为了模仿高星项目一次引入多 Agent、可视化工作流、消息队列和 Kubernetes。
- 为了复用少量代码建立跨模块循环依赖。
- 未完成的功能提前写进简历或 README 的“已实现功能”。

## 5. 增量重构原则

- 不做一次性大搬家。开发某个功能时，只重构该功能触及的模块。
- 移动代码前先补或确认回归测试，移动后保持接口行为不变。
- 新模块通过构造参数或 FastAPI 依赖注入获得依赖，避免在业务代码中到处创建全局客户端。
- 共享接口优先使用小而稳定的 Protocol 或服务方法，不暴露第三方库对象。
- 任何数据库结构变化都通过 Alembic 迁移，不手工修改生产表。
- 涉及 MySQL、文件和 Chroma 的组合写入必须设计失败回滚和一致性验证。
- 跨 MySQL、文件和 Chroma 不存在真正的原子事务，文档写操作使用共享生命周期服务和可补偿流程，不复制多套实现。
- 授权只通过集中的角色策略完成；`require_admin` 逐步兼容 `admin/super_admin`，新增能力优先使用小型 `require_roles` 或 Policy，禁止在路由、页面和多个 Service 中散落角色判断。
- JWT 只识别用户，数据库角色才是授权真相源；禁止信任前端传入的角色或仅靠隐藏按钮保护接口。
- 管理员模块不得修改会话、RAG Prompt、SSE 协议或普通用户隔离逻辑。
- 新功能必须先写清输入、输出、失败策略和所属模块，再开始编码；没有模块归属的功能不得直接塞进现有 Service。
- 任何大任务先拆成单模块、可测试、可回退的小任务；每次只允许一个主模块发生行为变化。

## 6. 前端产品与视觉约束

前端目标是安静、清晰、适合反复操作的企业应用，不是营销落地页。企业感来自信息架构、权限、状态、反馈和一致性，不来自大面积渐变、巨型标题或装饰动画。

- 使用统一 `AppShell`：桌面侧边导航、紧凑顶栏、面包屑、当前状态和账号菜单；移动端改为抽屉导航。
- 普通用户、管理员和超级管理员按权限显示不同工作台，但页面权限最终由后端控制。
- 表格用于文档、任务、用户和审计记录；详情使用抽屉或对话框，禁止在卡片里再嵌套卡片。
- 卡片圆角不超过 8px；颜色同时使用中性色、品牌绿、操作蓝、警告黄和危险红，避免单一青绿色铺满页面。
- 操作按钮优先使用 Element Plus 图标和工具提示；状态必须有加载、空数据、失败、无权限、降级、重试和成功反馈。
- 所有新增页面在 1440/1280 宽桌面和 390 宽移动端检查文字溢出、遮挡、横向滚动和交互可达性。
- 页面只组合 Feature 组件；API、权限、状态转换和 SSE 解析不得直接堆在大型 View 中。
- 对话式Agent的v3.1目标使用左侧会话与中间聊天两栏；公开计划、工具步骤、来源和产物
  作为对应助手消息的可折叠部件，详情按需打开，不保留永久右侧上下文栏。隐藏推理、
  Prompt和scratchpad不得进入页面。
- Agent消息顺序必须来自数据库thread内递增序号；`created_at`和随机UUID不能作为唯一
  顺序真相。REST历史与SSE必须按稳定实体ID幂等归并，禁止在View里简单拼接多套消息数组。
- 不为了展示技术栈提前引入 Pinia。只有跨页面共享的账号、角色、导航状态或持久筛选明显增长时再引入。

完整页面地图、角色视图和视觉规范见 `docs/product-and-ui-design.md`。

## 7. 安全和成本约束

- API Key、JWT 密钥和数据库密码只从环境变量或本地 `.env` 读取。
- `.env`、上传文件、Chroma 数据、日志和数据库备份不得提交 Git。
- 密码只能保存安全哈希，禁止明文密码进入数据库、日志和异常。
- 日志默认不记录完整问题、回答、Prompt、Token 或医学文档正文。
- 测试优先使用假模型和假 Embedding；只有明确需要真实验证时才调用 DashScope。
- 禁止无限重试、循环模型调用和没有上限的 Agent 工具循环。

## 8. 分级验证要求

验证范围跟随风险，不再让每个小任务机械运行全部检查：

- **L0 文档/注释**：检查链接、状态一致性、`git diff --check`；没有改命令、配置契约或生成资产时不运行应用测试。
- **L1 单模块实现**：运行直接相关测试文件，加一项最接近入口的回归测试。前后端只验证实际触及的一侧。
- **L2 共享链路**：修改公共 Port、认证、会话、文档生命周期、Redis策略、RAG主链路、API契约或数据库迁移时，运行完整后端；触及前端/API交互时再运行前端组件与SSE测试。
- **L3 发布/部署/数据迁移**：运行完整后端、完整前端、SSE、正式构建、依赖/迁移/数据一致性和敏感信息检查，并按任务做真实黑盒验收。

完整命令保留如下，仅在 L2/L3 或明确需要时执行：

~~~powershell
backend\.venv\Scripts\python.exe -m pytest -q backend/tests
D:\Nodejs\npm.cmd --prefix frontend test
D:\Nodejs\npm.cmd --prefix frontend run test:stream
D:\Nodejs\npm.cmd --prefix frontend run build
~~~

涉及页面交互时进行真实浏览器验收；涉及删除或迁移时验证目标数据和无关数据；涉及付费调用时遵守独立预算闸门。测试失败只读取首个有效分歧附近的输出，不反复加载全部成功日志。

## 9. 每次任务的交付格式

每次完成一个任务后必须：

1. 说明改了什么，以及没有改什么。
2. 用黑盒数据流解释功能如何工作。
3. 列出执行过的测试和结果。
4. 更新 `docs/handoff.md` 的当前状态、验证结果、阅读范围与唯一下一任务。
5. 只有阶段/任务状态变化时更新 `docs/development-roadmap.md`。
6. 只有接口、数据模型、模块边界或关键策略变化时更新 `docs/technical-design.md`；普通修复不得重复写一遍流水账。

一次只留下一个明确的“下一任务”，让新的开发窗口无需依赖旧聊天记录也能继续。
